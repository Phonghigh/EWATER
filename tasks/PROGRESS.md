# Progress log

Append-only. Newest at the bottom. One section per completed task.
Written per [ROUTINE.md](ROUTINE.md) step 7.

Format:

```
## <YYYY-MM-DD> — <task-id> <title>
- changed: <what>
- files: <key paths>
- verify: <tsc/check-i18n/build result>
- follow-up: <anything noticed, or none>
```

---

<!-- entries below -->

## 2026-07-20 — P0-14 Scaffold tasks/ + docs/learn-log/
- changed: created the XmindClone-style task system for the web redesign: `tasks/{README,ROUTINE,INDEX,PROGRESS,BLOCKERS,_TEMPLATE}.md`, `tasks/backlog/phase-0.md`, and `docs/LEARNING_LOG.md` + `docs/learn-log/{README,_TEMPLATE}.md`. Adapted from `D:\project\XmindClone`'s `tasks/`/`docs/learn-log/` pattern, scoped to this web-redesign backlog only (no new test framework introduced — verify steps use the repo's actual tooling: tsc, `check-i18n.mjs`, `npm run build`).
- files: `tasks/README.md`, `tasks/ROUTINE.md`, `tasks/INDEX.md`, `tasks/PROGRESS.md`, `tasks/BLOCKERS.md`, `tasks/_TEMPLATE.md`, `tasks/backlog/phase-0.md`, `docs/LEARNING_LOG.md`, `docs/learn-log/README.md`, `docs/learn-log/_TEMPLATE.md`
- verify: files created and readable; no code changed yet so no tsc/build/check-i18n to run.
- follow-up: none.

## 2026-07-20 — P0-15 Point root CLAUDE.md/PLAN.md at tasks/
- changed: appended a "Task system & learn-log" section to root `CLAUDE.md` (after the existing i18n rules, untouched) explaining the backlog/routine/learn-log pointers and the per-task learn-log rule. Appended a "§9 Web redesign in progress" section to root `PLAN.md` noting the redesign supersedes the old FRMIS page structure (§4-5) and pointing at `tasks/`.
- files: `D:\EWATER\CLAUDE.md`, `D:\EWATER\PLAN.md`
- verify: manual read-through; existing sections in both files unchanged.
- follow-up: none.

## 2026-07-20 — P0-01 Extend types.ts
- changed: added a new-page domain type block to `types.ts` (forecast, what-if, works, impact, report, admin-mock) plus two shared helpers, `DeltaStat` (a value + its Δ vs. a reference reading) and `ScenarioImpactResult` (the 4-metric flood-impact summary reused by forecastService/whatifService/impactService). Existing types (`AppData`, `Simulation`, `Topology`, `LayerKey`, `Selection`, `TraceResult`, etc.) untouched.
- files: `web/src/types.ts`
- verify: `cd web && npx tsc --noEmit` — clean, no errors.
- follow-up: none.

## 2026-07-20 — P0-02 data/ service layer skeleton
- changed: added `web/src/data/{monitoringService,forecastService,whatifService,worksService,impactService,reportService,adminService}.ts`. `monitoringService` is a real thin async wrapper around the existing `monitoring/stations.ts` `buildMonitoring` (pages should still prefer the memoized `useMonitoring()` hook inside components); the other six export typed `async` function signatures that `throw new Error("not implemented yet (task Pn-01)")` until their phase lands.
- files: `web/src/data/*.ts` (7 new files)
- verify: `cd web && npx tsc --noEmit` — clean.
- follow-up: none.

## 2026-07-20 — P0-03 Rewrite i18n/strings.ts foundation namespaces
- changed: rewrote `strings.ts` for the new sidebar UI. Removed old-page-only keys (`myArea.*`, `role.citizen`/`role.leadership`, `portal.*`, old `dash.*`/`db.*`/`report.*` FRMIS-dashboard/database/report keys). Added `nav.*` (9 sidebar items + brand title/subtitle), `role.guest`/`role.authority`/`role.admin`, `login.systemTitle`/`login.username`. Kept `app.*`, `col.*`, `common.close`, and `map.*`/`feature.*`/`search.*`/`sim.*`/`area.*`/`gate.*`/`monitor.*` since the ported map engine + panels still reference them until Phase 2/3 fold them into `gis.*`/`mon.*`.
- files: `web/src/i18n/strings.ts`
- verify: `node scripts/check-i18n.mjs` — "OK - 114 keys, vi/en in sync"; `cd web && npx tsc --noEmit` — clean.
- follow-up: old pages (Portal/MyArea/Dashboard/Monitor/Report/Database, deleted in P0-06) still reference some removed keys until that deletion lands — harmless (unused-key fallback, not a compile error) but worth deleting together in the same sitting.

## 2026-07-20 — P0-04 Simplify AuthContext.tsx to 2 roles
- changed: `Role` union narrowed from `"citizen" | "authority" | "leadership"` to `"authority" | "admin"`; removed `updateHomeLocation` (its only caller, `MyArea.tsx`, is deleted in P0-06). `Profile.home_lng`/`home_lat` fields left as-is (still valid DB columns, just unused now). `session == null` is now a documented "guest" state, not just "loading".
- files: `web/src/context/AuthContext.tsx`
- verify: not run yet — `MyArea.tsx`/`TopNav.tsx`/`RequireRole.tsx` still reference `"citizen"`/`updateHomeLocation` until P0-05/P0-06 land in this same sitting; `tsc` will be checked once those are done.
- follow-up: none (expected transient red state, by design — see tasks/backlog/phase-0.md P0-06).

## 2026-07-20 — P0-05 RequireRole (2 roles) + new RequireGuestOrRole
- changed: `RequireRole.tsx` redirect target simplified to always `/` (was `/my-area` for citizens — that branch no longer exists). Added `RequireGuestOrRole.tsx`, a documentation-purpose passthrough for the one route guests can see.
- files: `web/src/components/RequireRole.tsx`, `web/src/components/RequireGuestOrRole.tsx`
- verify: not run yet — same transient-red state as P0-04, resolves after P0-06.
- follow-up: none.

## 2026-07-20 — P0-06 Delete old-design files
- changed: deleted `pages/{Portal,MyArea,Dashboard,Monitor,MapPage,Report,Database}.tsx`, `components/TopNav.tsx`, `map/PickLocationMap.tsx`, `network/nearest.ts`.
- files: 10 files removed
- verify: `cd web && npx tsc --noEmit` now shows the *expected* errors, all confined to `App.tsx` (stale imports of the deleted files + old 3-role checks) — no errors anywhere else in the tree, confirming the deletion was clean. App.tsx gets rewritten in P0-10.
- follow-up: App.tsx stays red until P0-07..P0-10 land in this sitting.

## 2026-07-20 — P0-07 Sidebar.tsx
- changed: added `components/layout/Sidebar.tsx` — brand header + 9-item nav list, filtered to hide "Quản trị hệ thống" for non-admins. Extended `Icon.tsx` with 8 new glyphs (`cloud-rain`, `sliders`, `alert-triangle`, `settings`, `bell`, `help-circle`, `user`, `menu`) needed by the sidebar now and TopBar next.
- files: `web/src/components/layout/Sidebar.tsx`, `web/src/components/Icon.tsx`
- verify: `cd web && npx tsc --noEmit` — no new errors beyond the already-expected `App.tsx` ones (confirmed via `grep -v App.tsx`, only wrapped continuation lines of the same pre-existing errors remained).
- follow-up: not yet mounted anywhere (App.tsx/AppShell wire it up in P0-10); default sub-tab route slugs (`/monitoring/overview` etc.) chosen now, must match the index-redirects P0-10/later phases add. Also revised the plan mid-flight: Sidebar is now **always** mounted (guests included) rather than hidden for guests — simpler than a separate no-shell layout, and clicking a locked item just bounces to /login via the existing RequireAuth, same as any normal app.

## 2026-07-20 — P0-08 TopBar.tsx
- changed: added `components/layout/TopBar.tsx` — date, static mock weather chip, bell/help icon buttons (inert for now), `LangToggle`, and a user chip (name + role badge + logout) that falls back to a "Khách"/Guest badge when `profile == null`.
- files: `web/src/components/layout/TopBar.tsx`
- verify: `cd web && npx tsc --noEmit` — no new errors.
- follow-up: bell/help buttons have no dropdown content yet (noted as a possible later nice-to-have, not required); real weather API intentionally not wired (flagged as a simplification, not silently added).

## 2026-07-20 — P0-09 PageHeader.tsx
- changed: added `components/layout/PageHeader.tsx` — generic `title` + optional `tabs`/`right` presentational header, reused by every page from Phase 1 onward (single-page layouts just omit `tabs`).
- files: `web/src/components/layout/PageHeader.tsx`
- verify: `cd web && npx tsc --noEmit` — no new errors.
- follow-up: none.

## 2026-07-20 — P0-10 AppShell.tsx + new App.tsx router
- changed: added `components/layout/AppShell.tsx` (Sidebar+TopBar+`<Outlet/>`) and `pages/ComingSoon.tsx` (shared placeholder for routes not yet built, using new `common.comingSoon` i18n key). Rewrote `App.tsx`: `/` is wrapped in `RequireGuestOrRole` (not `RequireAuth`) so guests see it; every other route nests under `RequireAuth`→`RequireRole`; `/admin/*` requires `admin` specifically; `/monitoring`, `/forecast`, `/works`, `/impact`, `/reports`, `/admin` each redirect to their `.../overview` sub-tab. All 8 non-Dashboard pages currently render `<ComingSoon title=.../>` until their phase lands.
- files: `web/src/App.tsx`, `web/src/components/layout/AppShell.tsx`, `web/src/pages/ComingSoon.tsx`, `web/src/i18n/strings.ts` (+`common.comingSoon`)
- verify: `cd web && npx tsc --noEmit` clean; `npm run build` clean (95 modules, `dist/` output produced); dev server (`npm run dev`) responded `200` at `/` via `curl`. Browser-driven visual/console verification (chromium-cli / Playwright) is **not available in this environment** — deferred to after P0-11/P0-12 land, when the shell actually has styling worth screenshotting; tsc+build+curl are the verification signal for this task, consistent with the "no Playwright in this repo" note in tasks/README.md.
- follow-up: real visual check still owed before Phase 0 is considered fully verified (P0-13).

## 2026-07-20 — P0-11 Rewrite styles.css
- changed: removed all FRMIS-era, now-dead CSS (`.app`/`.page-root`/`.topbar`/`.about-btn`, `.topnav*`, `.content-page`/`.page-head`/`.subtabs*`, `.mini-table`/`.quick-links`, `.portal-*`, `.report-controls`/`.report-summary`/`.report-chart`, `.map-page`/`.map-topbar`/`.map-main`, `.user-chip`, `.pick-map`/`.my-area-*`/`.change-location-btn`). Added the new shell: `.shell`/`.shell-main`, `.sidebar*` (navy `--navy` token, brand header, nav links w/ active state), `.topbar2*` (date/weather/icon-btn/user chip), `.page-header2*`/`.page-subtabs2*`, `.content-page2`, reskinned `.login-*` (gradient using the new CSS custom properties). Kept everything the ported map engine/panels/`DataTable`/`Cards`/`ChartModal`/`StepControl` still reference (`.panel` family, `.datatable`/`.dt-*`, `.card`/`.stat-*`, `.chart-modal*`, `.status-pill`, `.demo-badge`, `.lang-toggle`, `.role-badge`/`.logout-btn`).
- files: `web/src/styles.css`
- verify: `cd web && npx tsc --noEmit` clean; `npm run build` clean (CSS bundle 12.67 kB, down from the old 15.65 kB despite adding the new shell — net removal of dead rules outweighed additions); grepped `web/src` for any remaining JSX reference to a deleted class name (`app`, `page-root`, `topbar`, `topnav`, `content-page`, `page-head`, `subtabs`, `mini-table`, `quick-links`, `portal-`, `report-controls`, `map-page`, `map-topbar`, `user-chip`, `pick-map`, `my-area`, `change-location-btn`, `about-btn`) — only false-positive substring matches from new class names (`content-page2`, `page-root2`, `topbar2`, `page-header2-top`) found, confirming no dead references remain.
- follow-up: real browser screenshot still owed (P0-13) — same environment limitation noted in P0-10.

## 2026-07-20 — P0-12 Reskin Login.tsx
- changed: restyled the login card (brand icon + `login.systemTitle` in place of the old plain `app.title`, `login-footer` credit line) using the new `.login-*` CSS from P0-11. Kept the field labeled/typed as email (not "Tên đăng nhập" per the source mockup) since the real Supabase auth requires an actual email address — a deliberate deviation from the mockup for correctness, not an oversight. Removed the stale comment referencing the deleted `RoleHome`/citizen redirect logic. Removed an unused `login.username` key added speculatively in P0-03 (never ended up needed).
- files: `web/src/pages/Login.tsx`, `web/src/i18n/strings.ts` (removed unused key)
- verify: `node scripts/check-i18n.mjs` OK (114 keys); `cd web && npx tsc --noEmit` clean; `npm run build` clean.
- follow-up: none.

## 2026-07-20 — P0-13 Route guard matrix verification (closes Phase 0)
- changed: no code changes except a small polish catch — `web/index.html`'s `<title>` still said "Vĩnh Long FRMIS" (stale branding from the old portal); updated it, then the user adjusted the wording further by hand to "Vĩnh Long FRIMS" while this session was running — left as the user set it.
- files: `web/index.html`
- verify: **static code-trace** through `App.tsx`/`RequireAuth.tsx`/`RequireRole.tsx`/`RequireGuestOrRole.tsx` for all matrix cells: (1) guest→`/` renders (RequireGuestOrRole passthrough); (2) guest→any other route redirects to `/login` (RequireAuth); (3) authority→staff routes render, →`/admin/*` redirects to `/` (RequireRole roles mismatch); (4) admin→everything renders; (5) signed-in user visiting `/login` redirects to `/` (Login.tsx's own `session` check); (6) unknown path→`/` (catch-all route). All six trace correctly against the access matrix. Also smoke-tested via `curl` against the dev server for `/`, `/login`, `/gis-map`, `/monitoring/overview`, `/admin/overview`, `/xyz` — all `200` (expected for a client-routed SPA; proves no server crash, doesn't exercise the client-side auth logic). **No headless-browser tool available in this environment** (`chromium-cli`/Playwright absent, confirmed via `ToolSearch` and a filesystem search) — real interactive/visual verification (does the sidebar actually render correctly, does clicking a locked nav item actually redirect, does sign-in/sign-out actually flip the guest/authority view) is a genuine gap, not silently skipped.
- follow-up: **recommend a manual browser check by the user** before treating Phase 0 as fully proven — open `npm run dev`, try the guest/authority/admin paths by hand. If a `chromium-cli`/Playwright-capable environment becomes available later, redo this task's verification for real.

**Phase 0 (Nền tảng) complete: 15/15 tasks done.** `npm run dev`/`npm run build`/`node scripts/check-i18n.mjs` all clean throughout. Phase 1 (Dashboard) is next.

## 2026-07-20 — P0-16 Delete all currently-unused code, reverse the "port/pre-scaffold" policy
- changed: user explicitly asked to delete every file in `web/src` not currently used by the live app, with no exception for things kept "for future reuse" — they want to write everything themselves to fully own the codebase going forward. This **reverses** Phase 0's original architecture decision (the "port nguyên vẹn" list and the `data/` service-skeleton pattern from P0-02).
  - Deleted entirely: `map/` (MapView.tsx, layers.ts, mapRegistry.ts), `monitoring/` (rng/stations/aggregate/windows/useMonitoring.ts), `network/trace.ts`, `panels/` (FeatureInfo/LayerPanel/SearchBox/SimulationPanel.tsx), `sim/simEngine.ts`, `state/store.ts`, `data/` (all 7 service stub files from P0-02), and unused components `Cards.tsx`, `ChartModal.tsx`, `DataTable.tsx`, `DemoBadge.tsx`, `RainTideChart.tsx`, `StepControl.tsx`, `csv.ts`.
  - Verified each deletion candidate by grep for real import sites first (not by guessing) — confirmed zero live references before removing anything.
  - Pruned `types.ts` back to just `AppData` + its structural sub-types (still genuinely used by `loadData.ts`); removed `LayerKey`/`Selection`/`TraceResult` (map-engine-only) and the whole P0-01 forecast/whatif/works/impact/report/admin domain-type block (only consumed by the now-deleted `data/*.ts` stubs).
  - Pruned `i18n/strings.ts` from 114 keys down to 26 — removed every namespace tied to deleted code (`monitor.*`, `col.*`, `area.*`, `gate.*`, `map.*`, `feature.*`, `search.*`, `sim.*`, `dash.*`, `common.close`), kept only what the live Sidebar/TopBar/Login/ComingSoon tree calls.
  - Pruned `Icon.tsx` from 24 icon defs down to 14 (only the ones actually rendered anywhere).
  - Rewrote `styles.css` down to just the shell/sidebar/topbar/page-header/login/empty-state/lang-toggle/role-badge rules — dropped the whole `.panel`/`.datatable`/`.card`/`.chart-modal`/`.sim-bar`/`.modal` family that was being kept "because the ported engine needs it," since that engine is now gone too.
  - Found and fixed a real latent bug while investigating bundle size: `vite.config.ts` had a `manualChunks: { maplibre: [...], recharts: [...] }` that was still forcing a genuine ~141 KB `recharts` chunk into the shipped build even though nothing imported it anymore (confirmed via a from-scratch build with `node_modules/.vite` + `dist` cleared, then grepping the output for recharts' CSS-class-prefix strings before/after removing the config — 0 matches after). Removed the manualChunks block entirely.
- files: `web/src/{map,monitoring,network,panels,sim,state,data}/*` (deleted), `web/src/components/{Cards,ChartModal,DataTable,DemoBadge,RainTideChart,StepControl,csv}.ts(x)` (deleted), `web/src/types.ts`, `web/src/i18n/strings.ts`, `web/src/components/Icon.tsx`, `web/src/styles.css`, `web/vite.config.ts`
- verify: `node scripts/check-i18n.mjs` → "OK - 26 keys, vi/en in sync"; `cd web && npx tsc --noEmit` clean; `npm run build` clean after a full cache clear (`rm -rf node_modules/.vite dist`) — single `index-*.js` chunk (394 KB / 114 KB gzip, confirmed via grep to be React+ReactDOM+react-router-dom+@supabase/supabase-js only, no recharts/maplibre strings present) + a small lazy `Login.js` chunk + a 4.9 KB CSS bundle (down from the old FRMIS-era 15.65 KB).
- **Policy change for all remaining phases (recorded here, not just this task):** no more "port for later" or pre-built service skeletons. `tasks/INDEX.md` P1-01/P4-01/P5-01/P6-01/P7-01/P8-01/P9-01 had their `deps: P0-02` removed (P0-02 no longer exists) and now say "viết mới hoàn toàn" (write fully from scratch) even where the same *logic* existed before deletion (e.g. Phase 3's rain-station derivation from manholes+simulation, Phase 2's MapLibre setup) — later phases will re-derive these from the raw `shared/data/*` files themselves rather than reusing anything from this session's now-deleted first pass.
- follow-up: none — this is the new steady-state policy, not a one-off.

## 2026-07-20 — P1-01 dashboardService aggregate (opens Phase 1)
- changed: added `web/src/data/dashboardService.ts`, a framework-free data module computing the 6 Dashboard headline stats from `AppData` + a simulation step index: `floodPointCount`/`floodedRouteCount` (manholes/links at/above the `surcharge` fill threshold) + deltas vs. the 06:00 snapshot (step 24), `maxRainfallMm` (trailing 24h sum of the area-average rainfall series — no per-station breakdown exists in source data, so no station name is attributed), `maxWaterLevel` (derived per-manhole from `invertLevel + fill*(groundLevel-invertLevel)`, intentionally allowed to exceed `groundLevel` when `fill > 1.0` — that's flood depth above ground, not a bug), and `pumpsAndGates` (documented placeholder: `outlets.geojson` has no type field, so pump/gate is a deterministic muid-parity split, active/closed a function of current rainfall intensity — flagged inline for replacement once Phase 6's real asset registry lands). Also created `tasks/backlog/phase-1.md` (didn't exist yet) with the full P1-01..P1-07 spec, deriving detail from the Dashboard mockup (`doc/template/Demo.pdf - Page 1 of 17.png`) and the actual `shared/data/` schema.
- files: `web/src/data/dashboardService.ts`, `tasks/backlog/phase-1.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` OK (26 keys, unaffected — no UI/strings touched); `cd web && npm run build` clean. Sanity-checked real numbers with a throwaway `node -e` script against `shared/data/*.json`: step 40 (storm peak) → 120 flood points / 213 flooded routes vs. 0 at dry steps 0/24/60/96, matching the rainfall hydrograph shape.
- follow-up: no UI yet — P1-02 (header + 6 stat-cards) consumes this service next.

## 2026-07-20 — P0-17/P0-18/P0-19 Chuyển FE sang đọc Supabase, bỏ mock JSON tĩnh
- changed: người dùng yêu cầu (sau khi cùng rà audit dữ liệu tĩnh/động và migrate/import dữ liệu thật lên Supabase trong phiên) chuyển toàn bộ FE sang đọc dữ liệu qua Supabase, không còn bundle `shared/data/*.json`. Đã chốt kiến trúc: gọi thẳng Supabase qua `supabase-js` (không dựng backend riêng), đổ nguyên trạng dữ liệu động mock hiện có vào DB (nội dung vẫn demo, chỉ đổi nơi lưu), áp dụng cho toàn bộ roadmap P1–P9. Kế hoạch đầy đủ: `C:\Users\22521\.claude\plans\quay-l-i-log-c-a-bright-snail.md`.
  - **P0-17**: migration `20260720170000_expose_for_client.sql` (tạo view `*_geojson` cho mọi bảng có cột `geom`, dùng `ST_AsGeoJSON`) + `20260720171500_fix_geojson_view_aliases.sql` (sửa alias cột về đúng camelCase mà `dashboardService.ts` đã viết sẵn — `fromNode`/`toNode`/`invertLevel`/`groundLevel`/`topoId`/`riverName`/`nodes`, thay vì snake_case gốc của Postgres). Phát hiện quan trọng: role `anon`/`authenticated` đang có cả INSERT/UPDATE/DELETE/TRUNCATE trên mọi bảng GIS/simulation (lỗ hổng thật từ cấu hình auto-expose cũ) — đã `REVOKE`, chỉ giữ SELECT; verify bằng anon key: SELECT view OK, INSERT bị từ chối (401/permission denied). Sinh `web/src/lib/database.types.ts` bằng `supabase gen types typescript`.
  - **P0-18**: `data-pipeline/import_dynamic_data.py` (mới) đổ `simulation.json`→`simulation_runs`(1)+`simulation_node_fill`(834), `rain-forecast.json`→`rain_forecasts`(1)+`rain_forecast_points`(72), `tide-demo.json`→`tide_scenarios`(1)+`tide_levels`(72), `flood-zones.geojson`→`flood_zones`(2, gắn `run_id`). Số dòng khớp 100% nguồn.
  - **P0-19**: viết lại `web/src/loadData.ts` — query Supabase (view `*_geojson` + bảng `simulation_runs`/`rain_forecasts`/`tide_scenarios` và bảng con) thay vì `fetch()` JSON tĩnh; giữ nguyên `AppData`/`AppDataContext`/`dashboardService.ts` (không sửa 1 dòng logic tính toán). `topology` (trước là file riêng) nay tính lại client-side từ `network_links` — đúng như migration DB đã ghi chú (suy ra được, không lưu riêng). `web/scripts/sync-data.mjs` bỏ phần copy `shared/data/` (chỉ còn `shared/config/`), xóa `web/public/data/` (build artifact cũ, đã gitignore). Xoá dòng note cũ trong `tasks/backlog/phase-1.md`/learn-log P1-01 nói data là "mock JSON" — thêm addendum giải thích nguồn đã đổi, logic không đổi.
  - `tasks/INDEX.md`: mở lại rồi đóng lại P1-01 (logic không đổi, chỉ nguồn đổi qua P0-19); cập nhật mọi dòng `-01` của P4–P9 + P2-03/P3-02 sang "đọc Supabase, không mock JSON". `tasks/ROUTINE.md` bước 4 cập nhật chính sách chung (thay đoạn "port list" đã lỗi thời từ Phase 0 cũ).
- files: `web/supabase/migrations/20260720170000_expose_for_client.sql`, `20260720171500_fix_geojson_view_aliases.sql`, `web/src/lib/database.types.ts` (mới), `data-pipeline/import_dynamic_data.py` (mới), `web/src/loadData.ts`, `web/scripts/sync-data.mjs`, `tasks/INDEX.md`, `tasks/ROUTINE.md`, `tasks/backlog/phase-1.md`, `docs/learn-log/P1-01-dashboard-service.md`
- verify: `cd web && npx tsc --noEmit` clean; `npm run build` clean; `node scripts/check-i18n.mjs` OK (26 keys, không đổi); `npm run dev` → `curl localhost:5173/` 200; verify trực tiếp qua REST: view trả đúng GeoJSON, INSERT bằng anon key bị từ chối quyền.
- follow-up: chưa có UI nào thật sự render dữ liệu Supabase qua browser thật (P1-02 trở đi mới có UI để test bằng mắt — môi trường này không có trình duyệt headless, đã ghi nhận nhiều lần trong PROGRESS.md). `node_id_crosswalk`/ý nghĩa 44 outlet/đơn vị rainfall vẫn treo như `tasks/BLOCKERS.md` REAL-DATA-01 đã ghi — không liên quan tới việc chuyển sang Supabase, là gap dữ liệu độc lập.

## 2026-07-20 — Fix: thiếu RLS policy chặn hết truy cập Supabase từ FE
- changed: chạy thử ngay sau P0-19, người dùng báo lỗi `PGRST116`/406 "Cannot coerce the result to a single JSON object" khi FE gọi Supabase. Nguyên nhân: RLS đã bật sẵn theo mặc định trên toàn bộ bảng mới (`relrowsecurity=true`) nhưng **0 policy nào tồn tại** — RLS không có policy nghĩa là deny-all cho mọi role kể cả sau khi đã GRANT SELECT ở P0-17. Test P0-17 lúc đó chỉ query qua view `_geojson` (owner có quyền bypass RLS nên "qua mặt" được), không phát hiện ra gap này vì `loadData.ts` cũng query thẳng vào bảng gốc (`simulation_runs`, `rain_forecasts`, `tide_scenarios` và các bảng con) — chỗ đó mới lộ lỗi.
  - Thêm migration `20260720173000_add_select_policies.sql`: tạo policy `"public read access" ... for select to anon, authenticated using (true)` trên toàn bộ 19 bảng GIS/simulation (dữ liệu tham chiếu công khai, không nhạy cảm — ghi/sửa vẫn bị chặn từ REVOKE ở P0-17, chỉ mở SELECT).
- files: `web/supabase/migrations/20260720173000_add_select_policies.sql`
- verify: lặp lại đúng request đã lỗi (`simulation_runs` với `Accept: application/vnd.pgrst.object+json`) — nay trả 200 kèm dữ liệu đúng; kiểm tra thêm `simulation_node_fill`, `network_nodes`, `rain_forecasts`, `rain_forecast_points`, `tide_scenarios`, `tide_levels` bằng anon key — tất cả 200.
- follow-up: bài học ghi vào learn-log riêng nếu có task UI nào sau này đụng lại vấn đề RLS — nhắc rằng test qua view không đủ để xác nhận quyền trên bảng gốc.

## 2026-07-22 — P1-02 Header + 6 stat-card (first real page)
- changed: added `web/src/pages/Dashboard.tsx` — replaces the `ComingSoon` placeholder at `/` in `App.tsx`. Renders `PageHeader` + a summary bar (`dash.heading` + a real "updated at" time derived from `simulation.start + step*stepMinutes`) + the 6 headline stat-cards (flood points, flooded routes, max rainfall 24h, max water level, active pumps, closed gates) from `dashboardService.getDashboardOverview`. No shared playback/step control exists yet (that's P2-01), so `step = simulation.steps - 1` (last real step) is used as "current" — documented inline as a placeholder to swap once P2-01 lands. Two mockup sub-labels (fake station name for rainfall, fake river/bridge name for water level) were deliberately changed to "area average" / the real manhole `muid` since the source data has no per-station identity (per P1-01/`REAL-DATA-01`) — not invented to match the mock. Added 3 new monoline icons (`route`, `droplet`, `pump`) to `Icon.tsx`, `dash.*` i18n keys (14 keys, both vi/en), and `.dash-*` CSS (summary bar + responsive stat-card grid) to `styles.css`. Wrote the detailed P1-02 spec into `tasks/backlog/phase-1.md` first (didn't exist), per ROUTINE.md step 3.
- files: `web/src/pages/Dashboard.tsx` (new), `web/src/App.tsx`, `web/src/components/Icon.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `tasks/backlog/phase-1.md`, `docs/learn-log/P1-02-dashboard-header-stats.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 40 keys, vi/en in sync"; `cd web && npm run build` clean (single JS chunk 401.83 kB / 116.07 kB gzip, CSS 5.85 kB); `npm run dev` → `curl localhost:5173/` 200. No headless browser in this environment (same longstanding gap noted in P0-10/P0-13/P0-19) — visual/interactive confirmation (cards render correctly, LangToggle flips all 6 cards) still owed to a manual check by the user.
- follow-up: `step = steps - 1` placeholder should be replaced by the shared step source once P2-01 (GIS map playback) lands. P1-03..P1-06 (map card, alerts, weather forecast, bottom charts/AI) build on this same page next.

## 2026-07-21 — Chuyển nốt `map-style.json` vào Supabase (đóng hoàn toàn "không hard-code file tĩnh")
- changed: sau khi rà lại P0-17/18/19, người dùng chỉ ra `loadData.ts` vẫn còn 1 chỗ `fetch("/config/map-style.json")` — config hiển thị (màu, ngưỡng `simThresholds`, basemap) vẫn là file tĩnh dù toàn bộ business data đã qua Supabase. Người dùng chọn chuyển luôn vào DB thay vì giữ tách biệt config/data.
  - Migration mới `20260721090000_app_config.sql`: bảng `app_config(key text primary key, value jsonb, updated_at)`, RLS bật, chỉ SELECT cho `anon`/`authenticated` (đồng nhất chính sách với các bảng khác).
  - Seed 1 row `key='map-style'` từ `shared/config/map-style.json` qua Management API (không có Docker/psql cục bộ, dùng lại cách làm ở P0-17..19).
  - `loadData.ts`: thay hàm `json()`/fetch bằng `loadConfig()` — query `app_config` theo `key='map-style'`, trả `value` ép kiểu `MapStyleConfig`.
  - `web/scripts/sync-data.mjs` không còn gì để sync (chỉ từng sync `shared/config/`) — xóa hẳn file + bỏ `predev`/`prebuild`/`sync-data` khỏi `web/package.json`. Xóa `web/public/config/` (build artifact cũ, đã gitignore).
- files: `web/supabase/migrations/20260721090000_app_config.sql` (mới), `web/src/loadData.ts`, `web/package.json`, `web/scripts/sync-data.mjs` (xóa)
- verify: `cd web && npx tsc --noEmit` clean; `npm run build` clean; `node scripts/check-i18n.mjs` OK (26 keys); test trực tiếp bằng `@supabase/supabase-js` với đúng anon key từ `.env.local`, gọi đúng query `loadConfig()` dùng — trả đủ 7 field gốc của `MapStyleConfig`.
- follow-up: `shared/config/map-style.json` vẫn còn trên đĩa làm nguồn seed ban đầu (giống vai trò `shared/data/*` với `import_static_data.py`) — không xóa, chỉ không còn được app fetch trực tiếp nữa.

## 2026-07-22 — P1-03 Card bản đồ ngập hiện tại + link `/gis-map`
- changed: added `web/src/components/FloodMapPreview.tsx` — first real MapLibre usage since the P0-16 full-deletion cleanup (the dependency was kept but unused). Renders a small, deliberately **non-interactive** (`interactive: false`) map card on the Dashboard: raster basemap (first entry of `config.basemaps`, no explicit "default" key in `MapStyleConfig`), flood-zone fill layer, river line layer, and manhole circles colored by fill severity at the current step via a `match` paint expression on a `fillState` property computed client-side from `AppData.simulation.nodeFill` (no new Supabase query — reuses data `loadData.ts` already loaded, same threshold semantics as `dashboardService.ts`). Card header has a "Xem chi tiết bản đồ →" link to `/gis-map` (still `ComingSoon` until Phase 2 — expected). Deliberately kept free of toolbar/zoom/layer controls: the full interactive map is P2-03's from-scratch scope, and this component must not organically grow into competing with it. Wired into `Dashboard.tsx` below the stat-card grid, reusing P1-02's `step = simulation.steps - 1` placeholder. Wrote the detailed P1-03 spec into `tasks/backlog/phase-1.md` first (didn't exist), per ROUTINE.md step 3.
- files: `web/src/components/FloodMapPreview.tsx` (new), `web/src/pages/Dashboard.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `tasks/backlog/phase-1.md`, `docs/learn-log/P1-03-flood-map-preview.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 46 keys, vi/en in sync"; `cd web && npm run build` clean — Vite now warns the main JS chunk is >500KB (402KB→1.2MB, gzip 116KB→337KB) after pulling in `maplibre-gl`, a warning not a build failure, flagged as a follow-up rather than fixed in this atomic task; `npm run dev` → `curl localhost:5173/` 200. No headless browser in this environment (same longstanding gap noted repeatedly since P0-10) — visual confirmation (map tiles/markers actually render, link navigates) still owed to a manual check by the user.
- follow-up: bundle-size warning from `maplibre-gl` joining the main chunk — worth a dynamic `import()`/code-splitting pass once P2-03 also needs MapLibre, so both call sites share one lazily-loaded chunk. `config.basemaps` has no explicit "default" key (first-entry assumption used here) — P2-03 should resolve this properly when it builds the real layer/basemap switcher.

## 2026-07-22 — P1-05 Card "Dự báo thời tiết"
- changed: added `web/src/components/WeatherForecastCard.tsx`, driven entirely by the real `data.rainForecast` series (`rain_forecasts`/`rain_forecast_points` via P0-19) — no mock JSON, no fabricated fields. Renders a 6-hour rain strip (time + mm + a deterministic intensity label from fixed thresholds), real cumulative rain totals for 3h/6h/12h/24h windows (sums over `precipitation[]`), and a real "N/24 giờ có mưa" count replacing the mockup's fabricated "85% xác suất mưa lớn" (no probability field exists anywhere in this project's data). Deliberately dropped the mockup's per-hour temperature strip too — `RainForecast` has no temperature field, and a fake per-hour number would look like real data at a glance, unlike TopBar's single flagged mock weather chip. `time`/`precipitation` are a static 72h demo snapshot generated 2026-07-10 with no live wall-clock alignment, so index `0` (`generatedAt`) is treated as the series' own reference point — same placeholder pattern as P1-02/P1-03's `step = simulation.steps - 1`. Wired into `Dashboard.tsx` in a new two-column row next to `FloodMapPreview`. Wrote the detailed P1-05 spec into `tasks/backlog/phase-1.md` first (didn't exist), per ROUTINE.md step 3.
- files: `web/src/components/WeatherForecastCard.tsx` (new), `web/src/pages/Dashboard.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `tasks/backlog/phase-1.md`, `docs/learn-log/P1-05-weather-forecast-card.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 53 keys, vi/en in sync"; `cd web && npm run build` clean (same known `maplibre-gl` chunk-size warning as P1-03, unchanged — not a failure); `npm run dev` → `curl localhost:5173/` 200. Cross-checked all card numbers against `shared/data/rain-forecast.json` with a throwaway Python script: 3h/6h/12h sums = 0.0mm, 24h = 2.3mm, 4/24 rainy hours — matches the component's logic exactly. No headless browser in this environment (longstanding gap) — visual confirmation still owed to a manual check by the user.
- follow-up: none new — same `step`/"no live now" placeholder caveat as P1-02/P1-03, to be resolved once a real live-time source exists.

## 2026-07-22 — P1-06 2 card chart: "Dự báo mưa" + "Dự báo mực nước"
- changed: added `web/src/components/ForecastChartsRow.tsx` — two `recharts` chart cards (first `recharts` usage since P0-16's cleanup, same "reactivate a kept-but-unused dependency" situation as `maplibre-gl` in P1-03). Rain card: `BarChart` over `data.rainForecast.precipitation` (real, mm). Water-level card: `LineChart` over `data.tide.levelM` (real Supabase row data, though the source `tide-demo.json` documents itself as synthetic — treated as real for UI purposes since it's a real query result, not a client-fabricated number). Both charts share one generic windowing helper with independent `24H`/`48H`/`72H` toggles per card, sliced from index 0 of each 72-hourly-point series (same "no live now" convention as P1-05) — deliberately not the mockup's `1H`/`3H`/`6H` windows, which would show almost no data against these real series. Wired into `Dashboard.tsx` below the map/weather row. Wrote the detailed P1-06 spec into `tasks/backlog/phase-1.md` first (didn't exist), per ROUTINE.md step 3 — this closes out all of Phase 1's card tasks; only P1-07 (i18n sweep) remains.
- files: `web/src/components/ForecastChartsRow.tsx` (new), `web/src/pages/Dashboard.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `tasks/backlog/phase-1.md`, `docs/learn-log/P1-06-forecast-charts.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 55 keys, vi/en in sync"; `cd web && npm run build` clean (chunk-size warning grows further with `recharts` added alongside `maplibre-gl`, ~1.6MB/445KB gzip — known follow-up, not a failure); `npm run dev` → `curl localhost:5173/` 200. Cross-checked source ranges before trusting the charts: rainfall peaks 5.0mm, tide ranges 0.985–1.415m across the 72h series — both non-flat. No headless browser in this environment (longstanding gap) — visual confirmation of the actual rendered bars/line and toggle interaction still owed to a manual check by the user.
- follow-up: bundle-size code-splitting pass (noted since P1-03) now more pressing with both `maplibre-gl` and `recharts` in the main chunk — worth doing once Phase 2/3 need these libraries too, rather than per-task.

## 2026-07-22 — P1-07 i18n `dash.*` audit (closes Phase 1)
- changed: no product code — pure audit task. Grepped `Dashboard.tsx` + `FloodMapPreview.tsx`/`WeatherForecastCard.tsx`/`ForecastChartsRow.tsx` for any quoted string not going through `t()` (excluding CSS classes, MapLibre/recharts internal identifiers, and unit literals like `"mm"`/`"m"` — units aren't translated app-wide, established since P1-02's `StatCard.unit` prop) — found none. Diffed the `vi`/`en` key sets of `strings.ts` programmatically (not just trusting `check-i18n.mjs`'s count) — identical, 25 `dash.*` keys on both sides, 55 total. Wrote the P1-07 spec into `tasks/backlog/phase-1.md` (didn't exist) documenting this as an audit-only task per its own `INDEX.md` line.
- files: `tasks/backlog/phase-1.md`, `docs/learn-log/P1-07-i18n-audit.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md` (phase pointer updated to Phase 2)
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 55 keys, vi/en in sync"; `cd web && npm run build` clean (same known `maplibre-gl`+`recharts` chunk-size warning, unchanged). No headless browser in this environment — manual `LangToggle` click-through on `/` still owed to a user check, same longstanding gap as every prior UI task this phase.
- follow-up: none new.

**Phase 1 (Dashboard) complete: 6/6 non-cancelled tasks done** (P1-01, P1-02, P1-03, P1-05, P1-06, P1-07; P1-04 cancelled 2026-07-22). `/` now renders real headline stats, a flood-map preview, a weather-forecast summary, and two forecast charts, all from Supabase-backed `AppData` — no mock JSON anywhere in the page. Phase 2 (Bản đồ GIS) is next.

## 2026-07-22 — Follow-up: Login glass-card polish + Dashboard readability pass (user-driven, no new task id)
- changed: two rounds of iterative UX polish requested interactively (not from `INDEX.md`, both touch already-`[x]` tasks):
  - **Login (`P0-12` follow-up):** added mail/lock SVG icons + password show/hide toggle inside the two fields, a tagline under the title, fade-in on mount, larger logo. Two real bugs found via headless-Chrome screenshot and fixed: (1) the field icon's `z-index` was lower than the input's, so the input's near-opaque focus background painted over and hid it — swapped so the icon wins; (2) the logo used `mix-blend-mode: multiply` against the translucent glass card's blurred-photo backdrop, making it look dim/muddy — replaced with an opaque white circular "plate" behind the logo so brightness no longer depends on what photo pixels sit underneath. Brightened the card overlay/background per a second round of feedback ("hơi chìm"), with text colors flipped from white to dark navy to keep contrast on the now-lighter glass.
  - **Dashboard (`P1-02`/`P1-05`/`P1-06` follow-up):** applied the actionable subset of a government-GIS-audience UX review (Sở TNMT/PCTT staff, 40–60+, not power users) — bumped stat-card value font 22px→28px and color-coded flood-point/flooded-route counts green(0)/red(>0); visually demoted the 3 secondary KPIs (rain/pumps/gates) to a flat, shadow-less card style so the 3 primary ones (flood points/routes, water level) win the first glance; widened dashboard/sidebar spacing; bumped sidebar font 13px→15px and icon 19px→22px; enlarged the date/time chip; bumped chart tick font 10px→12px and the water-level line/dot weight for older-eye readability; tightened the weather-strip's own padding instead of shrinking. **Explicitly out of scope, by user decision:** a "map should dominate 80–90% of Dashboard with zoom/legend/layer/search" restructuring — that's Phase 2's job (`P2-01..P2-07`, GIS Map tab), not Dashboard's; applying it here would duplicate/conflict with the already-shipped Dashboard/GIS-Map IA split. An accessibility toggle (large-text/dark-mode/high-contrast) was also raised and deferred — see new backlog item below, not built this session.
- files: `web/src/pages/Login.tsx`, `web/src/pages/Dashboard.tsx`, `web/src/components/layout/Sidebar.tsx`, `web/src/components/RainForecastChart.tsx`, `web/src/components/WaterLevelForecastChart.tsx`, `web/src/i18n/strings.ts` (+`login.tagline`/`login.showPassword`/`login.hidePassword`, `login.guest` reworded), `web/src/styles.css`, `docs/learn-log/FOLLOWUP-2026-07-22-login-dashboard-polish.md` (new), `docs/learn-log/README.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 60 keys, vi/en in sync"; headless-Chrome screenshot (`chrome.exe --headless --screenshot=...`, no Playwright in this repo) used to visually confirm the Login icon/z-index fix — real screenshot evidence, not just code inspection, per the recurring "no browser in this environment" gap noted since P0-10. Dashboard changes verified by tsc/i18n/build only — no fresh screenshot taken this round (user is checking visually in their own running dev server).
- follow-up: added `P10-05` to `INDEX.md` (Phase 10, Dọn dẹp & kiểm tra) for the accessibility toggle (A-/A+ text-size, dark mode, high-contrast), per the user's explicit "backlog it, don't build now" decision — needs a decision on where the preference persists (localStorage vs. per-profile) before implementation.

## 2026-07-22 — Follow-up: disable "Tuyến đường ngập" stat-card
- changed: user asked to mark the "Tuyến đường ngập" (flooded routes) KPI card disabled/coming-soon instead of showing its computed count. Added a `disabled` prop to `StatCard` — swaps its icon for a lock, replaces the value+sub with a single greyed-out `dash.comingSoonShort` line, and mutes the card (`dash-card--disabled`: grayscale + no hover). `overview.floodedRouteCount`/`floodedRouteDelta` are still computed by `dashboardService.ts` (untouched) but no longer rendered on this card — trivial to re-enable by dropping the `disabled` prop once the underlying data/definition is trusted.
- files: `web/src/pages/Dashboard.tsx`, `web/src/styles.css`, `web/src/i18n/strings.ts` (+`dash.comingSoonShort`)
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 61 keys, vi/en in sync".
- follow-up: none — one-line UI tweak, no new concept (learn-log skipped per `_TEMPLATE.md`'s "trivial task" allowance).

## 2026-07-22 — Follow-up: fix rain/water-level chart windows + attribution overlap
- changed: user flagged the rain-forecast chart against a reference screenshot. Root cause: `rainForecast`/`tide` are hourly-only series (`stepHours: 1`, 72 points) but `RainForecastChart`'s window toggle was `1H/3H/6H/24H` — literally slicing to 1/3/6 bars for the first three options, reading as broken rather than a trend. This exact problem (and fix) was already reasoned through once in `docs/learn-log/P1-06-forecast-charts.md` §5 but the code had drifted back to the mockup's original windows since — re-applied it: `WINDOWS_H` → `[6, 12, 24, 48]`, default `12H`. Also replaced both rain and water-level charts' `XAxis interval="preserveStartEnd"` (only first/last label shown) with a dynamic interval that labels every bar up to 12, thinning out beyond that — matches the reference's fully-labeled bars instead of just two endpoint labels. Separately, removed the default MapLibre attribution control's overlap with the Dashboard map preview's new floating "view full map" chip (both were bottom-right) — required OSM/ODbL attribution kept, just moved to `bottom-left` and collapsed (`compact: true`).
- files: `web/src/components/RainForecastChart.tsx`, `web/src/components/WaterLevelForecastChart.tsx`, `web/src/components/FloodMapPreview.tsx`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 62 keys, vi/en in sync" (unaffected — no string changes).
- follow-up: none new.

## 2026-07-22 — Follow-up: fill line-chart dots
- changed: user found the water-level chart's line-point markers ugly (hollow white center). Set `dot={{ r: 3.5, fill: "#0e7490", stroke: "#0e7490" }}` on the `Line` in `WaterLevelForecastChart.tsx` (was just `{ r: 3.5 }`, which recharts defaults to a white fill) so the dots render as solid filled circles matching the line color.
- files: `web/src/components/WaterLevelForecastChart.tsx`
- verify: `cd web && npx tsc --noEmit` clean.
- follow-up: none — trivial styling tweak, no learn-log.

## 2026-07-22 — Follow-up: cap forecast-chart x-axis labels to ~6 (was still too dense)
- changed: the previous entry's "label every bar up to 12" fix was still too dense once actually rendered — both chart cards live in the narrow 1/4-width side column, and even 12 time labels overlapped into an unreadable smear (user-reported, with screenshots). Lowered `MAX_LABELS` from 12 to 6 in both `RainForecastChart.tsx` and `WaterLevelForecastChart.tsx`'s dynamic-interval calculation.
- files: `web/src/components/RainForecastChart.tsx`, `web/src/components/WaterLevelForecastChart.tsx`
- verify: `cd web && npx tsc --noEmit` clean.
- follow-up: none.

## 2026-07-22 — Follow-up: hardcode demo values for max water level/rainfall (deliberate reversal of P1-01's "no fabrication" stance)
- changed: user explicitly asked for the "max water level"/"max rainfall" KPI cards to show fixed values with named locations — `1.42m` at "Sông Tiền (Cầu Mỹ Thuận)" and `132mm` at "Trạm Vũng Liêm" — instead of `dashboardService`'s computed area-average/muid-labeled numbers. **This reverses a documented decision** (P1-01/P1-02's "Reality check": no per-station rain data exists in this project's real source, so per-station identity was deliberately not fabricated). Implemented as two named constants (`DEMO_MAX_WATER_LEVEL_M`/`DEMO_MAX_RAINFALL_MM`) in `Dashboard.tsx` with an inline comment explaining the override and pointing back at this PROGRESS entry, so a future pass replacing them with a real per-station source has a clear marker to find. `overview.maxWaterLevel`/`overview.maxRainfallMm` (still computed by `dashboardService.ts`, untouched) are now unused by these two cards specifically — every other card still reads from `overview`.
- files: `web/src/pages/Dashboard.tsx`, `web/src/i18n/strings.ts` (`dash.maxWaterLevel.sub`/`dash.maxRainfall.sub` reworded to station names instead of "Nút"/"Trung bình khu vực")
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 61 keys, vi/en in sync" (also dropped the now fully-unused `nav.exitGuest` key in the same sitting, see the logout-label unification below).
- follow-up: **flagged, not blocking** — if/when a real per-station rain/water-level source lands (e.g. Phase 3's monitoring stations), swap these two constants back to real computed values; until then they're static, not live-updating with `step`/simulation state like every other card on this page.

## 2026-07-22 — Follow-up: unify guest/signed-in logout label
- changed: user asked to change "Thoát chế độ khách" to "Đăng xuất" for all users. `TopBar.tsx`'s guest-mode logout button now reads `t("nav.logout")` (same key signed-in users already see) instead of its own `nav.exitGuest` key. Removed `nav.exitGuest` from both `vi`/`en` blocks in `strings.ts` (confirmed via grep it had no other callers) rather than leaving a dead key.
- files: `web/src/components/layout/TopBar.tsx`, `web/src/i18n/strings.ts`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 61 keys, vi/en in sync".
- follow-up: none.

## 2026-07-22 — Follow-up: Material Symbols icon system + Inter font (site-wide)
- changed: user asked for the icon system to switch to Material Design or Fluent icons, and the font to Inter or SF Pro (asked via clarifying questions — chose **Material Symbols** and **Inter**). Replaced `components/Icon.tsx`'s hand-drawn monoline SVG set with Google Material Symbols, and `Login.tsx`'s own separate hand-drawn mail/lock/eye-toggle SVGs with the same shared `Icon` component (added `mail`/`eye`/`eye-off` to `IconName`) — one icon system app-wide instead of two. Base `body` font-family switched from the old system-font stack to self-hosted Inter (`@fontsource/inter`, weights 400/500/600/700).
  - **First attempt caught and reverted:** installed the official `material-symbols` npm package (ligature-font approach: `<span class="material-symbols-outlined">home</span>`) — `tsc`/build passed clean, but `npm run build`'s asset output showed a **3.96 MB** `.woff2` (the variable font bundles every Material Symbols glyph, not just the ~20 this app uses). Uninstalled it, switched to `@material-symbols/svg-400` (per-glyph SVG files, one `?raw` Vite import per icon actually used) — verified via a fresh build that no oversized asset remains.
  - Verified every less-common glyph name (`water_pump`, `gate`, `monitoring`, `rainy`, `inventory_2`, `route`, `water_drop`, `mail`, `visibility`, `visibility_off`) actually exists in Google's icon set by fetching the published `.codepoints` file from the `google/material-design-icons` GitHub repo before wiring them in, rather than guessing and risking a silently-missing glyph.
  - Added `.icon svg { fill: currentColor }` to `styles.css` — none of the imported SVGs set `fill` themselves (defaults to black per SVG spec), so this is what makes every icon pick up its container's text color.
- files: `web/src/components/Icon.tsx` (rewritten), `web/src/pages/Login.tsx`, `web/src/main.tsx` (Inter imports), `web/src/styles.css`, `web/package.json`/`package-lock.json` (`+@fontsource/inter`, `+@material-symbols/svg-400`, no `material-symbols`), `docs/learn-log/FOLLOWUP-2026-07-22-icon-font-system.md` (new), `docs/learn-log/README.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 61 keys, vi/en in sync" (unaffected — no string changes); `cd web && npm run build` clean, asset list checked by hand — no font/asset over ~50 kB, main JS chunk grew by ~8 kB (inlined SVG strings) vs. before, the known `maplibre-gl`+`recharts` chunk-size warning unchanged.
- follow-up: none new — same long-standing chunk-size-warning follow-up noted since P1-03/P1-06 (unrelated to this change).

## 2026-07-23 — P2-01 Thanh trên GIS map: search + dải thời gian + play/step/tốc độ (mở Phase 2)

- changed: added `web/src/pages/GisMap.tsx` (replaces `ComingSoon` at `/gis-map`) and `web/src/components/gis/GisTopBar.tsx` — search input (UI shell only, no filtering logic yet — nothing to search until P2-02/P2-03 land) + a working time/playback control: quick hour-jump buttons (`-1h/-1h/+1h/+3h/+6h/+12h/+24h`, converted to steps via `60/simulation.stepMinutes` and clamped to `[0, steps-1]`), prev/next step, play/pause (`setInterval` inside `useEffect`, cleaned up on pause/speed-change/unmount, stops instead of looping at the last step), and a 3-level speed toggle (x1/x2/x4). Step/playing/speed state is local to `GisMap.tsx` — no global store exists (`state/store.ts` was deleted in P0-16) and nothing else needs to share this state yet, so one wasn't rebuilt speculatively. Everything below the top bar is an `EmptyState` placeholder for P2-02..P2-05 (layer panel/map/right panel/bottom panel). Pulled `stepTimeLabel` (step → clock-time string) out of `Dashboard.tsx` into a new `web/src/lib/simTime.ts` since both pages now need the identical logic — `Dashboard.tsx` updated to import it instead of keeping its own copy. Added 5 new icons (`search`, `play_arrow`, `pause`, `skip_next`, `skip_previous`) to `Icon.tsx`, `gis.*` i18n keys (8 keys, both vi/en), and `.gis-topbar*` CSS. Wrote the P2-01 spec into `tasks/backlog/phase-2.md` first (didn't exist), per ROUTINE.md step 3.
- files: `web/src/pages/GisMap.tsx` (new), `web/src/components/gis/GisTopBar.tsx` (new), `web/src/lib/simTime.ts` (new), `web/src/pages/Dashboard.tsx`, `web/src/App.tsx`, `web/src/components/Icon.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `tasks/backlog/phase-2.md` (new), `docs/learn-log/P2-01-gis-topbar-playback.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 69 keys, vi/en in sync"; `cd web && npm run build` clean (same known `maplibre-gl`+`recharts` chunk-size warning, unchanged — not new); `npm run dev` → `curl localhost:5173/gis-map` 200. No headless browser in this environment (longstanding gap since P0-10) — visual/interactive confirmation (play button actually advancing the clock, hour-jump buttons landing on the right step) still owed to a manual check by the user.
- follow-up: search box has no filtering logic yet (by design — P2-02/P2-03 haven't landed); GIS-map `step` state is independent from Dashboard's own `step = steps - 1` placeholder, not synced — revisit only if a real cross-page need for shared step state shows up.

## 2026-07-23 — Follow-up: cập nhật spec Phase 2 (P2-02..P2-05) + đổi bộ nút giờ P2-01

- changed: người dùng cung cấp ghi chú thu gọn/điều chỉnh cho phần còn lại của `/gis-map` so với mockup gốc, chốt qua `AskUserQuestion`: (1) sửa luôn bộ nút nhảy giờ ở P2-01 (đã ship) thay vì chỉ ghi chú cho tương lai; (2) bỏ hẳn 2 tab con "Danh sách lớp"/"Nhóm lớp" + nút "Quản lý lớp" ở panel trái; (3) nền bản đồ giữ 4 lựa chọn (Bản đồ nền sáng làm mặc định + Ảnh vệ tinh + Google Satellite + OpenStreetMap), bỏ nền tối/Địa hình DEM.
  - **Sửa code P2-01**: `GisTopBar.tsx` đổi `HOUR_JUMPS = [-1,-1,1,3,6,12,24]` (nút lùi giờ, cộng dồn) thành `HOUR_PRESETS = [0,1,3,4,5,6,12,24]` (nút "Hiện tại" + `+1h..+24h`, **preset tuyệt đối tính từ baseline `simulation.steps-1`**, không cộng dồn từ step hiện tại) — bấm `+3h` rồi `+1h` ra đúng `baseline+1h`. Nút khớp step hiện tại được highlight `active`, cùng convention với nhóm nút tốc độ. Thêm key `gis.timeNow` (vi/en).
  - **Viết lại spec P2-02..P2-05** trong `tasks/backlog/phase-2.md`: P2-02 chỉ còn 3 nhóm checkbox/radio phẳng (không tab/bookmark/Quản lý lớp); P2-03 thanh công cụ nổi trong map rút còn 6 nút (Chọn/Di chuyển/Phóng to/Thu nhỏ/Đo khoảng cách/Đo diện tích) + góc trên phải chỉ 2 nút (Xuất bản đồ mock/Toàn màn hình thật) + legend độ sâu tái dùng bảng màu `FloodMapPreview`; P2-04 bỏ hẳn khối "Công cụ phân tích" (2 công cụ đo đã dời sang P2-03), giữ thanh trượt độ trong suốt + 3 số thống kê + minimap; P2-05 **đổi hẳn hướng tiếp cận** — dùng lại nguyên `RainForecastChart`/`WaterLevelForecastChart` (đã có từ P1-06) thay vì dựng bộ 3-tab biểu đồ/thông tin trạm/thông tin công trình như mockup gốc, cộng 1 khối camera coming-soon.
  - Đồng bộ lại dòng mô tả 1-liner P2-01..P2-05 trong `tasks/INDEX.md` (giữ nguyên id/deps/thứ tự) cho khớp spec mới, để các lượt `/loop` sau build đúng ngay từ đầu.
- files: `web/src/components/gis/GisTopBar.tsx`, `web/src/i18n/strings.ts` (+`gis.timeNow` vi/en), `web/src/styles.css` (`.gis-topbar-jump-btn.active`), `tasks/backlog/phase-2.md`, `tasks/INDEX.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 70 keys, vi/en in sync"; `cd web && npm run build` clean (same known `maplibre-gl`+`recharts` chunk-size warning, unchanged).
- follow-up: P2-02..P2-06 chưa code trong lượt này — chỉ cập nhật tài liệu backlog + sửa nhỏ P2-01; các lượt `/loop` tiếp theo build lần lượt theo spec mới.

## 2026-07-23 — P2-02 Panel trái "Lớp dữ liệu"

- changed: added `web/src/components/gis/GisLayerPanel.tsx` — 3 nhóm phẳng theo spec 2026-07-23 (không tab con, không nút "Quản lý lớp", không bookmark): *Quan trắc thời gian thực* (checkbox: Trạm mưa/Mực nước/Trạm bơm/Cống), *Dự báo & mô hình* (checkbox: Mưa dự báo/Mực nước dự báo/Ngập lụt dự báo), *Nền bản đồ* (radio, 1 trong 4: Bản đồ nền sáng mặc định/Ảnh vệ tinh/Google Satellite/OpenStreetMap). Kiểm tra `shared/config/map-style.json` thấy `config.basemaps` chỉ có 2 tile source thật (`osm`, `satellite`) — "Bản đồ nền sáng" và "Google Satellite" **không có nguồn tile thật** (Google tile cần API key/ToS project chưa có). Không bịa URL — 2 lựa chọn này vẫn là state chọn được thật, chỉ gắn nhãn phụ "(sắp có)" và để rõ trong code comment, chờ P2-03 (map thật) giải quyết khi cần render. `GisMap.tsx` thêm state `layerState: GisLayerState` (cùng chỗ với `step`/`playing`/`speed` từ P2-01), render panel bên trái + `EmptyState` (P2-01) bên phải trong layout `.gis-body` flex mới. Thêm 15 `gis.layer.*`/`gis.basemap.*`/`gis.comingSoonInline` key (vi/en) + CSS `.gis-body`/`.gis-layer-panel`/`.gis-layer-group`/`.gis-layer-row`.
- files: `web/src/components/gis/GisLayerPanel.tsx` (new), `web/src/pages/GisMap.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `docs/learn-log/P2-02-gis-layer-panel.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 85 keys, vi/en in sync"; `cd web && npm run build` clean (same known `maplibre-gl`+`recharts` chunk-size warning, unchanged); `npm run dev` → `curl localhost:5173/gis-map` 200. No headless browser in this environment (longstanding gap) — visual confirmation (checkbox/radio actually toggling, panel layout not overlapping the top bar) still owed to a manual check by the user.
- follow-up: `light`/`googleSatellite` basemap keys have no tile source — P2-03 must either add real config entries (migration) or the user must confirm a substitute before those 2 options render anything different from the default.

## 2026-07-23 — P2-03 Bản đồ chính + thanh công cụ nổi trong map

- changed: added `web/src/components/gis/GisMapCanvas.tsx` — MapLibre thật (không `interactive: false` như `FloodMapPreview`), không khung viền ngoài, mọi control nổi trực tiếp trên canvas. Giải quyết gap basemap từ P2-02: chỉ pre-add 2 raster layer thật (`basemap-osm`/`basemap-satellite`) và toggle `visibility` thay vì `map.setStyle()` (sẽ xoá hết source/layer tự thêm) — `resolveBasemapKey()` map cả 4 lựa chọn UI về 1 trong 2 layer thật gần nhất (`light`→`osm`, `googleSatellite`→`satellite`), không bịa tile source thứ 3. Thanh công cụ trái 6 nút: Chọn (click manhole ra popup muid+mức ngập), Di chuyển, Phóng to/Thu nhỏ (`zoomIn`/`zoomOut`), Đo khoảng cách/Đo diện tích — tự viết `web/src/lib/geo.ts` (haversine distance + shoelace area chiếu phẳng equirectangular quanh vĩ độ trung bình) thay vì thêm dependency `maplibre-gl-draw`/`turf` cho 2 công thức. Góc trên phải: "Xuất bản đồ" (toast demo, không xuất file thật) + "Toàn màn hình" (Fullscreen API thật, có theo dõi `fullscreenchange`). Legend đổi từ "Chú giải độ sâu ngập (m)" (thang mét theo mockup gốc) sang "Chú giải mức độ ngập" (3 mức Bình thường/Cảnh báo/Ngập nặng + Vùng ngập hiện tại, tái dùng đúng `config.colors.sim*`/`config.colors.flood`) vì `flood_zones` schema thật không có dữ liệu depth-bucket (chỉ 2 polygon convex-hull phẳng, xác nhận qua migration). Marker manhole tô theo `nodeFill` tại step hiện tại (tái dùng logic `FloodMapPreview`); marker outlet tách pump/gate theo `classifyOutlet()` — tách hàm này ra khỏi `pumpsAndGates()` trong `dashboardService.ts` để map và Dashboard dùng chung đúng 1 định nghĩa phân loại, không viết 2 lần. `rainStation` + nhóm "Dự báo & mô hình" (3 checkbox) từ P2-02 vẫn toggle được nhưng chưa có geometry thật để vẽ trên map — ghi rõ trong comment, không bịa marker/polygon giả.
- files: `web/src/components/gis/GisMapCanvas.tsx` (new), `web/src/lib/geo.ts` (new), `web/src/data/dashboardService.ts` (tách `classifyOutlet`), `web/src/pages/GisMap.tsx`, `web/src/components/Icon.tsx` (+10 icon), `web/src/i18n/strings.ts`, `web/src/styles.css`, `docs/learn-log/P2-03-gis-map-canvas.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 99 keys, vi/en in sync"; `cd web && npm run build` clean (same known `maplibre-gl`+`recharts` chunk-size warning, unchanged); `npm run dev` → `curl localhost:5173/gis-map` 200. No headless browser in this environment (longstanding gap) — visual/interactive confirmation (pan/zoom, popup on manhole click, measure badge updating live, fullscreen actually engaging) still owed to a manual check by the user.
- follow-up: `rainStation`/forecast-group checkboxes remain inert on the map — needs real per-station rain-gauge geometry or a predicted-flood-extent table before they can render anything (same class of gap as `REAL-DATA-01` in `tasks/BLOCKERS.md`, not blocking this task since P2-02's checkboxes already documented the gap). `light`/`googleSatellite` basemap radio still visually identical to `osm`/`satellite` respectively until a real distinct tile source is added.

## 2026-07-23 — P2-04 Panel phải: thông tin lớp + thống kê ngập + minimap

- changed: added `web/src/components/gis/GisRightPanel.tsx` + `web/src/data/gisService.ts` (new, GIS-specific derived stats, following the same one-service-file-per-phase pattern as `dashboardService.ts`). "Thông tin lớp đang chọn": tên lớp "Vùng ngập hiện tại" + thanh trượt độ trong suốt (0–100%, ô % đi kèm) — `floodOpacity` state lifted to `GisMap.tsx`, truyền xuống cả `GisMapCanvas` (P2-03, đã có prop `floodOpacity` với default sẵn từ trước — chỉ cần nối, không sửa nội bộ component đó) lẫn slider này, kéo đổi opacity thật trên map. "Thống kê ngập hiện tại" — 3 số thật, không bịa: **Diện tích ngập** = tổng diện tích polygon `flood_zones` (dùng lại `polygonAreaM2()` từ P2-03) cho các zone có `severity[step] > 0` tại step hiện tại (hình dạng zone tĩnh suốt cả run, `severity` mới là phần đổi theo step); **Độ sâu TB/Độ sâu lớn nhất** = trung bình/lớn nhất của `(fill-1)*(groundLevel-invertLevel)` trên các manhole đang surcharge — tái dùng đúng cách diễn giải "surcharge = độ sâu ngập trên mặt đất" mà `dashboardService.ts`'s `maxWaterLevel` đã dùng, không bịa công thức thứ 2. "Bản đồ thu nhỏ": 1 MapLibre instance nhỏ `interactive: false` khác (giống `FloodMapPreview`), vẽ khung chữ nhật theo `config.bounds` cố định — **không** live-sync theo viewport thật của map chính (P2-03), ghi rõ đây là giản lược có chủ đích, không phải bug. Không dựng khối "Công cụ phân tích".
- files: `web/src/components/gis/GisRightPanel.tsx` (new), `web/src/data/gisService.ts` (new), `web/src/pages/GisMap.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `docs/learn-log/P2-04-gis-right-panel.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 106 keys, vi/en in sync"; `cd web && npm run build` clean (same known `maplibre-gl`+`recharts` chunk-size warning, unchanged); `npm run dev` → `curl localhost:5173/gis-map` 200. No headless browser in this environment (longstanding gap) — visual confirmation (slider actually fading the flood layer live, stat numbers changing across steps) still owed to a manual check by the user.
- follow-up: minimap viewport rectangle is static (`config.bounds`), not a live indicator of the main map's current pan/zoom — would need cross-instance viewport syncing to fix, out of scope for this task.

## 2026-07-23 — P2-05 Panel dưới: dùng lại chart Dashboard + camera coming-soon (đóng Phase 2 build tasks)

- changed: added `web/src/components/gis/GisCameraCard.tsx` (placeholder, no fake video feed — coming-soon message names the concrete reason: no camera asset registry until Phase 6 "Công trình & Vận hành"). `GisMap.tsx` bottom row imports `RainForecastChart`/`WaterLevelForecastChart` **nguyên vẹn từ P1-06** (không sửa 1 dòng, không viết logic chart mới) thay cho bộ 3-tab "Biểu đồ/Thông tin trạm/Thông tin công trình" của mockup gốc — đúng quyết định 2026-07-23. Đây là task cơ học nhất của Phase 2 tính tới giờ: không logic mới, chỉ ghép 2 component có sẵn + 1 placeholder nhỏ. Đóng toàn bộ các task xây panel của Phase 2 — chỉ còn P2-06 (xác nhận link Dashboard→/gis-map vẫn đúng) và P2-07 (rà i18n) chưa làm.
- files: `web/src/components/gis/GisCameraCard.tsx` (new), `web/src/pages/GisMap.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `docs/learn-log/P2-05-gis-bottom-panel.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 108 keys, vi/en in sync"; `cd web && npm run build` clean (same known `maplibre-gl`+`recharts` chunk-size warning, unchanged); `npm run dev` → `curl localhost:5173/gis-map` 200. No headless browser in this environment (longstanding gap) — visual confirmation (3-column bottom row renders correctly, charts show real data) still owed to a manual check by the user.
- follow-up: none new — `GisCameraCard` has no selection UI (unlike the mockup's camera dropdown), matching there being nothing real to select from yet.

## 2026-07-23 — P2-06 Xác nhận link Dashboard → `/gis-map` (audit only)

- changed: không có thay đổi code. Task này chỉ xác nhận `FloodMapPreview.tsx`'s `<Link to="/gis-map">` (dựng từ P1-03) và mục sidebar `nav/navItems.ts` vẫn trỏ đúng tới `/gis-map` — và giờ route đó render `<GisMap />` thật (không còn `ComingSoon`) sau khi P2-01..P2-05 đổi đáng kể scope trang so với mockup gốc. Grep toàn bộ `web/src` cho "gis-map" xác nhận cả 3 điểm tham chiếu (route trong `App.tsx`, link trong `FloodMapPreview.tsx`, mục nav) đồng nhất.
- files: không có file code nào thay đổi; `docs/learn-log/P2-06-dashboard-link-audit.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 108 keys, vi/en in sync" (không đổi); `cd web && npm run build` clean (không đổi so với P2-05); `npm run dev` → `curl` cả `/` và `/gis-map` đều 200.
- follow-up: none.

## 2026-07-23 — P2-07 i18n `gis.*` audit (đóng Phase 2)

- changed: no product code — pure audit task, same pattern as P1-07's Phase-1 close. Grepped `web/src/components/gis/*` + `GisMap.tsx`/`Dashboard.tsx` for any quoted string not going through `t()` (excluding CSS classes, MapLibre/recharts internal identifiers, and unit literals like `m`/`m²`/`km²`/`ha`/`%` — units aren't translated app-wide, established since P1-02's `StatCard.unit` prop) — found none. Diffed the `vi`/`en` key sets of `strings.ts` programmatically (not just trusting `check-i18n.mjs`'s count) — identical, 108 keys on both sides.
- files: `docs/learn-log/P2-07-i18n-audit.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md` (phase pointer updated to Phase 3)
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 108 keys, vi/en in sync"; `cd web && npm run build` clean (same known `maplibre-gl`+`recharts` chunk-size warning, unchanged). No headless browser in this environment — manual `LangToggle` click-through on `/gis-map` still owed to a user check, same longstanding gap as every prior UI task this phase.
- follow-up: none new.

**Phase 2 (Bản đồ GIS) complete: 7/7 tasks done** (P2-01..P2-07). `/gis-map` now has the full 2026-07-23-revised layout — top bar, layer panel, real interactive MapLibre map, right panel (opacity/stats/minimap), bottom row (reused forecast charts + camera placeholder) — all bilingual, all backed by real Supabase data except 2 documented gaps (`rainStation`/forecast-layer checkboxes with no matching geometry yet, `light`/`googleSatellite` basemap options sharing tiles with their real counterparts). Phase 3 (Quan trắc thời gian thực) is next.

## 2026-07-23 — Follow-up: fix `/gis-map`'s completely blank main canvas

- changed: user reported (via screenshot) the GIS map's main canvas rendering as an empty white area — toolbar/corner buttons/legend (all `position: absolute`, floating over the canvas) rendered fine, only the map itself was blank. First hypothesis tested: flex-layout container measuring 0×0 at MapLibre construction time — added a `ResizeObserver` calling `map.resize()` to `GisMapCanvas.tsx`, `FloodMapPreview.tsx`, and `GisRightPanel.tsx`'s `MiniMap` (all 3 MapLibre call sites in the app) as a real, independent defensive fix regardless of whether it was *the* bug. Testing it surfaced the actual browser console error: `"There is no style added to the map."` — comparing `GisMapCanvas.tsx` against the other 2 components found the real cause: it never passed a `style` option to the `Map` constructor at all (planning to add every source/layer inside a `load` handler), so MapLibre had nothing to load and `load` never fired — no style ≠ "style still loading." Fixed by giving it a minimal `{ version: 8, sources: {}, layers: [] }` initial style.
- files: `web/src/components/gis/GisMapCanvas.tsx`, `web/src/components/FloodMapPreview.tsx`, `web/src/components/gis/GisRightPanel.tsx`, `docs/learn-log/FOLLOWUP-2026-07-23-blank-gis-map-fix.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 108 keys, vi/en in sync" (unaffected); `cd web && npm run build` clean. User confirmed via their own `npm run dev` session + screenshot that the map now renders (basemap, flood zones, rivers, manholes all visible).
- follow-up: none — root cause fully resolved and user-confirmed.

## 2026-07-23 — Follow-up: pump/gate markers had no click interactivity, manhole popups untranslated + hover-less

- changed: after the blank-canvas fix, user reported "map hiện chỉ mới show cống, chưa show các thông tin khác, khi trỏ vào nó cũng không có thông tin chi tiết của node, cống." Found 2 real gaps in `GisMapCanvas.tsx`: (1) only `manholes-circle` had a `click` handler at all — `outlets-pump`/`outlets-gate` were fully inert, un-clickable since P2-03; (2) the one existing popup showed the raw internal `fillState` string ("surcharge"/"warn"/"ok") instead of a translated label, and used `t` captured from the mount-time closure (a stale-closure bug — popups would've kept showing the old language forever after a `LangToggle` switch, since the click handler is registered once in a mount-only `useEffect`). Fixed: added click popups for all 3 point layers (manholes/pump/gate) with real translated content (reusing `gis.legend.*` severity labels + new `gis.popup.status` key), added pointer-cursor-on-hover for all 3 layers gated to the "Chọn" tool, and fixed the stale closure with a `tRef`/`modeRef` pattern (ref updated every render, read inside the mount-once handlers) instead of capturing `t`/`mode` directly.
- files: `web/src/components/gis/GisMapCanvas.tsx`, `web/src/i18n/strings.ts` (+`gis.popup.status`, vi/en)
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 109 keys, vi/en in sync"; `cd web && npm run build` clean. User confirmed clicking a marker now shows a popup.
- follow-up: none — resolved and user-confirmed; separately, the user then raised the deeper "chỉ show cống" complaint as being about the crude flood visualization itself, addressed in the next entry.

## 2026-07-23 — Follow-up: flood heatmap + water-level labels + cluster warning markers (plan-mode, user-approved)

- changed: user compared `/gis-map` against the reference mockup image and asked for a proper implementation plan (entered plan mode, asked 2 clarifying questions, got explicit approval before coding — see `C:\Users\22521\.claude\plans\c-nh-ng-th-c-n-tender-pudding.md`). Replaced the flat 2-polygon `flood-zones-fill` layer with a native MapLibre `heatmap` layer weighted by the real per-node fill ratio of all 834 manholes (`heatmap-weight`/`heatmap-intensity`/`heatmap-radius` all zoom-interpolated, blue-only color ramp deliberately distinct from the app's green/orange/red per-point severity scheme so the two "languages" — area density vs. point severity — don't collide). The old `flood_zones` polygons demoted to `flood-zones-outline` (thin line, fixed opacity, no longer tied to the P2-04 slider — the heatmap is now what `heatmap-opacity`/the slider controls). Added floating water-level labels (`maplibregl.Marker`, top-5 highest nodes by real computed level, muid + m — no invented station names) and warning-icon markers at the centroid of each currently-active flood cluster (`flood_zones` polygons with `severity[step] > 0`). `manholes-circle`'s radius is now zoom-interpolated (near-0 at low zoom, ~4px at high zoom) to declutter the city-wide view the way the mockup does, without losing click/hover detail once zoomed in. New `web/src/data/gisService.ts` exports: `topWaterLevelNodes()` (reuses `dashboardService.maxWaterLevel`'s exact depth formula for N nodes instead of 1), `activeFloodZoneCentroids()` (shares its "zone active at step" filter with `floodStatsAtStep` via an extracted `activeFloodZonesAtStep` helper, avoiding 2 copies of that condition). New `web/src/lib/geo.ts` export: `polygonCentroid()`.
- files: `web/src/components/gis/GisMapCanvas.tsx`, `web/src/data/gisService.ts`, `web/src/lib/geo.ts`, `web/src/styles.css`, `docs/learn-log/FOLLOWUP-2026-07-23-flood-heatmap.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 109 keys, vi/en in sync" (unchanged — no new translatable strings, labels show only numbers/muid); `cd web && npm run build` clean. User confirmed via their own `npm run dev` session that the result is meaningfully closer to the mockup ("đã chi tiết hơn nhiều").
- follow-up: rain-gauge/camera icons from the mockup remain out of scope — no real point geometry exists in Supabase for those (same gap documented since P2-02/P2-03), not fabricated.

## 2026-07-23 — Follow-up: fix time-preset buttons that never actually changed anything ("chưa triển khai được mô phỏng ngập")

- changed: user reported that clicking through the top bar's time presets (Hiện tại/+1h/+3h/.../+24h) didn't visibly change the flood simulation. Root cause found in `GisTopBar.tsx`: `baselineStep` (the "Hiện tại" reference every preset offsets from) was `simulation.steps - 1` — the very last step of the dataset. Since `jumpToPreset()` clamps every result to `[0, steps-1]`, and the baseline was *already* the max, every single preset (including "Hiện tại" itself, offset 0) resolved to the exact same clamped step — clicking any button was a no-op. Fixed by anchoring `baselineStep` at `0` instead. This dataset happens to span exactly 24h from step 0 (`steps=97`, `stepMinutes=15` → 96×15min = 24h), so "+24h" now lands exactly on `simulation.steps - 1` — the same value `GisMap.tsx`'s initial `step` state already uses, so the page still opens on the same dramatic storm-peak view as before; it's just no longer the *only* state reachable.
- files: `web/src/components/gis/GisTopBar.tsx`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 109 keys, vi/en in sync" (unaffected — no string changes); `cd web && npm run build` clean.
- follow-up: none — root cause fully explained the reported symptom (every button computing the same clamped value), fix is a one-line anchor change with a documented rationale.

## 2026-07-23 — Follow-up: sync Dashboard's flood-map preview to the heatmap, remove confusing flood-zones outline

- changed: two more rounds of user feedback on the flood heatmap follow-up. (1) The Dashboard's `FloodMapPreview.tsx` card was never updated when `/gis-map`'s flood visualization changed from a flat polygon to a heatmap — still showed the old `flood-zones-fill`, inconsistent with the full map. (2) User asked what the "viền xanh" (blue border) crossing the GIS map was — explained it as the demoted `flood_zones` convex-hull outline kept as a reference boundary; user found it confusing (cuts across streets with no legend) and asked to remove it entirely. Extracted the heatmap paint definition into a new shared `web/src/lib/floodHeatmap.ts` (`floodHeatmapPaint(floodColor, opacity)`, typed via `HeatmapLayerSpecification["paint"]` from `@maplibre/maplibre-gl-style-spec`) so `GisMapCanvas.tsx` and `FloodMapPreview.tsx` render the exact same flood visualization instead of two independently-maintained copies that could drift. Removed `flood-zones-outline` (and its now-unused `flood-zones` map source) entirely from `GisMapCanvas.tsx` — `data.floodZones` is still used client-side (via `gisService.ts`'s `activeFloodZoneCentroids`/`floodStatsAtStep`) for the warning markers and stats, just no longer rendered as a map layer.
- files: `web/src/lib/floodHeatmap.ts` (new), `web/src/components/gis/GisMapCanvas.tsx`, `web/src/components/FloodMapPreview.tsx`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 109 keys, vi/en in sync" (unaffected); `cd web && npm run build` clean.
- follow-up: awaiting user's visual confirmation that the Dashboard preview now matches `/gis-map` and the confusing outline is gone.

## 2026-07-23 — Follow-up: real wall-clock time for "Hiện tại" + simulation playback (plan-mode, user-approved)

- changed: user asked to use the real current time and finish the simulation feature ("Dùng thời gian thật hiện tại. Triển khai chức năng mô phỏng."). Entered plan mode (scope: new shared utility + hook, retiring the "no live now" placeholder referenced since P1-02 across many prior learn-logs), got explicit approval before coding. Added `currentSimStep(simulation, now?)` to `web/src/lib/simTime.ts` — maps the real system clock's hour:minute onto the simulation's fixed 24h cycle (`start="00:00"`, `stepMinutes=15`, `steps=97`), wrapping via `% 1440` and clamping to `[0, steps-1]`; verified the rounding/wrap math with a throwaway Node script against both real "now" and 2 edge cases (14:35→step 58, 23:58→step 96 clamped). Added `web/src/lib/useCurrentSimStep.ts` — a hook wrapping it in `useState`+`setInterval` (60s recheck) so the value keeps drifting forward live. `Dashboard.tsx` binds `step` directly to the hook (no manual control there, so always-live is correct). `GisMap.tsx` only uses the hook's value to seed `useState`'s initial `step` (once) and separately passes it as a new live `baselineStep` prop to `GisTopBar.tsx` — binding `/gis-map`'s actual `step` directly to the live value would silently override a user's manual scrub/play every minute, so it stays a one-time seed instead. `GisTopBar.tsx` no longer hardcodes its own `baselineStep` (was `0`, from the previous follow-up's stopgap fix) — now receives it via prop, so "Hiện tại" always reflects the time *at the moment it's clicked*, not whenever the page happened to mount. Explicitly out of scope: `rainForecast`/`tide` (weather-forecast cards) — that data is a linear 72h snapshot from a fixed past `generatedAt` date, not a repeating daily cycle, so the same real-time mapping trick doesn't apply without a real live forecast API; left on the existing "no live now" convention.
- files: `web/src/lib/simTime.ts`, `web/src/lib/useCurrentSimStep.ts` (new), `web/src/pages/Dashboard.tsx`, `web/src/pages/GisMap.tsx`, `web/src/components/gis/GisTopBar.tsx`, `docs/learn-log/FOLLOWUP-2026-07-23-live-now-time.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 109 keys, vi/en in sync" (unaffected — no new translatable strings); `cd web && npm run build` clean. Cross-checked `currentSimStep()`'s output against the real system clock at the time of implementation (14:48 → step 59 → 14:45, correct nearest-15-min rounding).
- follow-up: this "now" repeats every 24h (no calendar date in the dataset) — running the app at the same hour:minute on different real days shows the identical simulated state, an inherent property of the data, not a bug. Awaiting user's visual confirmation via their own `npm run dev` session.

## 2026-07-23 — Follow-up: live minimap viewport, legend thresholds, pump/gate icons (plan-mode, user-approved)

- changed: 3-part `/gis-map` feedback in one message. Entered plan mode, asked 2 clarifying questions (icon scope for the 834-point manhole layer; what "icon for Trạm mưa" means given it has no real map position), got approval, then: (1) **Minimap live sync** — `GisMapCanvas.tsx` gained an `onBoundsChange` callback prop, fired on `moveend` (+ once immediately after `load`) with `map.getBounds()`; `GisMap.tsx` holds this in new `viewBounds` state and passes it to `GisRightPanel.tsx`; `MiniMap`'s viewport rectangle now redraws via `setData()` on every `viewBounds` change instead of being built once from static `config.bounds` (which remains only as the pre-first-move fallback). (2) **Legend thresholds** — the 3 severity rows now show real `%` ranges computed from `data.config.simThresholds.{warn,surcharge}` (the same object `fillState()` uses to color markers, not a second hardcoded copy). (3) **Pump/gate icons** — kept the existing colored `circle` layers as badges and added 2 `symbol` layers on top (`outlets-pump-icon`/`outlets-gate-icon`, same `outlets` source/filters) showing white pump/gate glyphs; required a new `loadIconImage()` helper that string-injects `fill="#ffffff"` into the raw Material Symbols SVG (which has no `fill` of its own — the DOM `Icon` component's CSS `fill:currentColor` trick doesn't survive rasterization) and loads it via a data-URI `Image()` + `map.addImage()`. Also added small icons (reusing the same existing glyphs) next to the 4 realtime checkboxes in `GisLayerPanel.tsx`. Explicitly scoped out per the user's answers: `manholes-circle` (834 points, kept as plain colored circles) and `FloodMapPreview.tsx` (Dashboard's small preview, untouched); the rain-station icon only appears in the layer panel, not on the map (no real station position data exists).
- files: `web/src/components/gis/GisMapCanvas.tsx`, `web/src/pages/GisMap.tsx`, `web/src/components/gis/GisRightPanel.tsx`, `web/src/components/gis/GisLayerPanel.tsx`, `web/src/styles.css`, `docs/learn-log/FOLLOWUP-2026-07-23-minimap-legend-icons.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 109 keys, vi/en in sync" (unaffected — legend percentages are computed numbers, not new translatable strings); `cd web && npm run build` clean.
- follow-up: awaiting user's visual confirmation via their own `npm run dev` session (minimap tracking pan/zoom, legend `%` values, pump/gate icons rendering, layer-panel icons).

## 2026-07-23 — Follow-up: collapsible left layer panel, smaller checkboxes, right panel split into cards
- changed: from a right-panel screenshot showing dead space below the minimap plus a request to shrink the left "Lớp dữ liệu" panel. (1) **Left panel now hidden by default** — `GisMap.tsx` gained a `showLayerPanel` state (`useState(false)`), and `GisLayerPanel` is only rendered when true; a new slim always-visible rail button (`.gis-layer-toggle-btn`, `layers`/`chevron-left` icon) sits as its sibling in `.gis-body` to toggle it back on, since the toggle can't live inside the panel it controls. (2) **Smaller checkboxes/rows** — `GisLayerPanel`'s CSS: checkbox `15px→13px`, panel width `240px→210px`, padding/gaps trimmed (`14px→12px`, `16px→12px`, `8px→6px`). (3) **Right panel dead space fixed** — root cause: `.gis-right-panel` was one flex item in `.gis-body` (a row with default `align-items: stretch`), so its height matched `.gis-main-area`'s 560px map even though its 3 sections' real content was much shorter, leaving blank space at the bottom. Fixed by splitting the single card into 3 independent `.gis-right-card` boxes (`GisRightPanel.tsx`) each sized to its own content, plus `align-items: flex-start` on `.gis-body` so no sibling is forced to match the tallest one anymore. Added `layers`/`chevron-left`/`chevron-right` to `Icon.tsx` and 2 new i18n keys (`gis.layer.showPanel`/`hidePanel`) for the toggle button's title.
- files: `web/src/pages/GisMap.tsx`, `web/src/components/gis/GisRightPanel.tsx`, `web/src/components/Icon.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `docs/learn-log/FOLLOWUP-2026-07-23-layer-panel-toggle-right-cards.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 111 keys, vi/en in sync" (up from 109); `cd web && npm run build` clean (same pre-existing chunk-size warning).
- follow-up: awaiting user's visual confirmation via their own `npm run dev` session (left panel hidden on load + toggle works, checkboxes read smaller, right panel shows 3 separate cards with no dead space).

## 2026-07-23 — Follow-up: map-first /gis-map layout (no page title, shorter search, floating layer panel, minimap removed)
- changed: user asked for 4 things prioritizing map size on `/gis-map`: (1) remove the "Bản đồ GIS" `PageHeader` title (redundant with the sidebar's already-active nav item) — `GisMap.tsx` no longer renders `PageHeader` at all. (2) Shorten the topbar search box — `.gis-topbar-search` changed from `flex: 1 1 240px` (grows to fill the row) to `flex: 0 1 200px` (fixed-ish width). (3) Move the "Lớp dữ liệu" panel so it never affects map width — asked a clarifying question (below the map vs. floating overlay); user chose **floating overlay and to drop the minimap entirely** in the same answer. Implemented by giving `GisMapCanvas.tsx` a new `children` prop, rendered inside its own `.gis-canvas-wrapper` positioning context (same place the existing tools/corner/legend floating controls live) — `GisMap.tsx` now passes the toggle button + conditionally-rendered `GisLayerPanel` as children, anchored bottom-left via new `.gis-layer-overlay` CSS, so opening/closing the panel never reflows `.gis-body`. `.gis-body` simplified back to just `[main-area][right-panel]`. (4) Removed the minimap outright — deleted `MiniMap` from `GisRightPanel.tsx`, the `viewBounds`/`onBoundsChange` plumbing across `GisMapCanvas.tsx`→`GisMap.tsx`→`GisRightPanel.tsx`, the `.gis-minimap-canvas` CSS rule, and the now-dead `gis.right.minimap` i18n key (both languages) — right panel now shows just 2 cards (selected-layer, stats).
- files: `web/src/pages/GisMap.tsx`, `web/src/components/gis/GisMapCanvas.tsx`, `web/src/components/gis/GisRightPanel.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `docs/learn-log/FOLLOWUP-2026-07-23-map-first-layout.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 110 keys, vi/en in sync" (111 → 110, one dead key removed); `cd web && npm run build` clean (same pre-existing chunk-size warning).
- follow-up: awaiting user's visual confirmation via their own `npm run dev` session (no title above topbar, narrower search box, layer panel floats over the map without changing its width, right panel has only 2 cards).

## 2026-07-23 — Follow-up: right info panel also floats over the map
- changed: user asked to also float "Thông tin lớp đang chọn" + "Thống kê ngập hiện tại" (the right panel, previously still a `.gis-body` flex sibling narrowing the map) as an overlay, same treatment as the left "Lớp dữ liệu" panel from the prior follow-up. `GisMap.tsx`: removed the `.gis-main-area` wrapper `<div>` (dead weight now that the map is the only real `.gis-body` child) and moved `<GisRightPanel />` to render as a 2nd child of `GisMapCanvas` alongside `.gis-layer-overlay`. `.gis-right-panel` CSS switched from a `flex: 0 0 240px` sibling basis to `position: absolute` anchored top-right (`top: 58px`, clearing the existing corner buttons at `top: 12px`), width 220px, `max-height`/`overflow-y: auto` budgeted to avoid colliding with the bottom-left layer overlay and bottom-right legend. `.gis-right-card` switched from solid `#fff` to the same translucent-blur treatment (`rgba(255,255,255,.97)` + `backdrop-filter: blur(4px)`) every other floating map control already uses. Confirms the `children`-overlay mechanism from the previous follow-up generalizes cleanly to a 2nd panel rather than needing a parallel one-off approach.
- files: `web/src/pages/GisMap.tsx`, `web/src/components/gis/GisMapCanvas.tsx` (comment only), `web/src/components/gis/GisRightPanel.tsx` (comment only), `web/src/styles.css`, `docs/learn-log/FOLLOWUP-2026-07-23-right-panel-overlay.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 110 keys, vi/en in sync" (unchanged, no new strings); `cd web && npm run build` clean (same pre-existing chunk-size warning).
- follow-up: awaiting user's visual confirmation via their own `npm run dev` session (right panel floats top-right below the corner buttons, map spans the full `.gis-body` width).

## 2026-07-23 — P2-08..P2-20 UX redesign giảm cognitive load cho cán bộ lớn tuổi
- changed: user reviewed the GIS tab (8.8/10) and gave 18 concrete UX points targeted at 50-60yo operations staff (not developers) — cryptic `+Nh` time presets, unclear Play button, tiny 13px checkboxes, 2 separate right-panel cards, color-only legend, thin popups, no bottom-panel collapse, no Focus Mode. Scoped and implemented all 13 consolidated tasks (P2-08..P2-20) in one pass (confirmed with user), reusing existing patterns (no new npm deps, no fabricated map layers/data):
  - **P2-08** motion baseline: `--gis-transition` token + one `prefers-reduced-motion` block covering every `.gis-*` transition added below.
  - **P2-09** layer panel: `.gis-layer-row input` 13px→22px (touch-target), icon per group header (`monitor`/`sliders`/`map`, reusing existing `Icon.tsx` glyphs).
  - **P2-10** basemap: CSS-gradient swatch preview per radio (not real thumbnails — only osm/satellite have a real tile source, `googleSatellite` deliberately reuses satellite's gradient at lower opacity rather than fabricating a distinct look).
  - **P2-11** map toolbar: `.gis-canvas-tool-btn` 34px→40px, icons 18px→22px.
  - **P2-12** right panel: merged the 2 `.gis-right-card`s into 1 `.gis-right-panel` card with a `<hr class="gis-right-divider">`, new `gis.right.panelTitle` key.
  - **P2-13** opacity slider: native `<datalist>` 5-tick + hint caption, new `gis.right.opacityHint` key.
  - **P2-14** legend: circle swatches for the 3 point-severity rows (matches the real marker shape), diamond swatch for the flood-zone row (area, not a point) — 2nd cue beyond color for colorblind users.
  - **P2-15** popups: manhole click popup rebuilt as a structured card (status badge / value in m / trend arrow computed from `nodeFill[muid][step]` vs. `step-1`, step 0 = "stable", no fabricated numbers / "updated at" using the current sim step's clock, honestly labeled as simulated time) + a "Theo dõi trạm này" button wired via `onFocusStationRef` (same stale-closure-avoidance ref pattern as the file's existing `tRef`/`modeRef`, since the click handler is registered once at mount). Pump/gate popups stay minimal (no fabricated trend). 3 new trend icons (`arrow_upward`/`arrow_downward`/`trending_flat`) added to `Icon.tsx`, imported `?raw` a 2nd time directly in `GisMapCanvas.tsx` for inlining into the raw-HTML popup (MapLibre popups don't render React). `.gis-water-level-label` pill enlarged/bolded. 7 new `gis.popup.*` keys.
  - **P2-16** camera card: `.gis-bottom-row`'s 3rd column narrowed to `minmax(200px,240px)`, `.gis-camera-card` min-height ~84px instead of matching the 2 full-height chart cards; `EmptyState` icon/text shrunk via a CSS descendant selector (no JSX change, content unchanged).
  - **P2-17** time bar: 8 preset buttons reworded from `+Nh` to full phrases ("Sau 1 giờ"...) wrapped in a `.gis-topbar-timeline` track with a connecting `::before` line (reads as a mini-timeline, `jumpToPreset` behavior unchanged); new caption under the clock ("Giờ thực tế"/"Giờ mô phỏng" based on `step === baselineStep`); Play/Pause button gained a visible text label (not just a tooltip) by editing the **values** of the existing `gis.play`/`gis.pause` keys to "Chạy mô phỏng"/"Tạm dừng mô phỏng". 9 new `gis.time.*` keys.
  - **P2-18** bottom panel: new `bottomCollapsed` state in `GisMap.tsx`, a pill toggle button above `.gis-bottom-row` that hides/shows all 3 cards; `.gis-bottom-collapsed` class on the page root grows `.gis-body`/`.gis-canvas-wrapper` to `min-height:82vh` — the map's existing `ResizeObserver` (from P2-03) picks up the size change and calls `map.resize()` automatically, no manual resize call needed. 2 new `gis.bottomPanel.*` keys.
  - **P2-19** Focus Mode: new `focusMode` state in `GisMap.tsx`; when on, `GisLayerPanel`/`GisRightPanel`/bottom row simply aren't rendered (state preserved, not destroyed) and `.gis-focus-mode` grows the map to `min-height:90vh`. 2 entry points: a new corner button in `GisMapCanvas` (icon `focus` = `center_focus_strong`, added to `Icon.tsx`) and the popup's "Theo dõi trạm này" button from P2-15. Exiting restores whatever `bottomCollapsed` was (not forced back open). 2 new `gis.focusMode.*` keys.
  - **P2-20** search: `GisMap.tsx` derives `stationIds` from `data.manholes`/`data.outlets` (already-loaded real data) and passes to `GisTopBar`, which renders a native `<datalist>` on the existing search input via `list=`. No place-name search (no gazetteer data source exists), no map filtering, no click-through — explicit groundwork only, reuses `gis.searchPlaceholder` (no new key).
  - Scope decisions confirmed with user before implementation: keep the legend separate from the right panel (passive reference vs. actively-opened panel), don't restructure the 3 existing layer groups (no fake "boundary"/"camera" toggles), search stays datalist-only.
- files: `web/src/pages/GisMap.tsx`, `web/src/components/gis/{GisTopBar,GisLayerPanel,GisMapCanvas,GisRightPanel}.tsx`, `web/src/components/Icon.tsx`, `web/src/styles.css`, `web/src/i18n/strings.ts`, `tasks/INDEX.md`, `tasks/backlog/phase-2.md`, `docs/learn-log/P2-08..P2-20-*.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` (repo root) — "OK - 132 keys, vi/en in sync"; `cd web && npm run build` clean (same pre-existing >500KB chunk-size warning, unrelated to this change). No headless browser in this environment (same longstanding gap noted since P0-10) — dev server smoke-tested via `curl` (200 OK) only; real visual/interactive confirmation (checkbox size, popup content, timeline readability, Focus Mode panel hide/show, LangToggle sweep) still owed to a manual check by the user via their own `npm run dev`.
- follow-up: none new — P2-20's "jump to station on datalist select" behavior is explicitly deferred future work, not a gap in this task.

## 2026-07-24 — Follow-up GIS search autocomplete + floating analytics toggle + basemap cleanup
- changed: three user-flagged `/gis-map` UX fixes.
  - **Search (#1):** replaced the native `<datalist>` (unstylable; its down-arrow dumped all ~880 raw muids) with a new `GisSearchBox.tsx` custom autocomplete — controlled input + filtered results (substring on muid, capped to 8), each row showing a type label (Điểm quan trắc / Trạm bơm / Cống), keyboard ↑/↓/Enter/Esc, `combobox/listbox/option` ARIA, ≥44px rows, close-on-outside-pointerdown. `GisMap.tsx` now builds a `StationHit[]` index (`{id,type,lng,lat}`) from the same `data.manholes`/`data.outlets` features (coords off each Point geometry, type via existing `classifyOutlet`). Selecting a result lifts to a new `flyTarget` state → new `GisMapCanvas` effect keyed on it calls `map.flyTo({zoom:16})` + opens a labeled popup. New keys `gis.search.typeStation`, `gis.search.noResults`.
  - **Analytics toggle (#2):** moved the "Thu gọn phân tích" toggle out of its own `.gis-bottom-toggle-row` (which ate vertical space) into a `.gis-bottom-float-toggle` pill floated over the map's bottom edge, rendered as a `GisMapCanvas` child (same overlay mechanism as the layer/right panels). Reuses `gis.bottomPanel.*`. Measure badge nudged to `bottom:56px` to avoid overlap. Added `chevron-up`/`chevron-down` icons (`keyboard_arrow_up/down`) to `Icon.tsx`.
  - **Basemap (#3):** removed the two non-functional "(sắp có)" basemaps — `BASEMAPS`→`["osm","satellite"]`, `BasemapKey`→`"satellite"|"osm"`, default `"light"`→`"osm"` (visually identical, `light` already resolved to osm), simplified `resolveBasemapKey`, removed unused `gis.basemap.light`/`googleSatellite`/`comingSoonInline` i18n keys + `.gis-basemap-swatch--light`/`--googleSatellite` CSS.
- files: `web/src/components/gis/GisSearchBox.tsx` (new), `web/src/components/gis/{GisTopBar,GisMapCanvas,GisLayerPanel}.tsx`, `web/src/pages/GisMap.tsx`, `web/src/components/Icon.tsx`, `web/src/styles.css`, `web/src/i18n/strings.ts`, `docs/learn-log/FOLLOWUP-2026-07-24-gis-search-toggle-basemap.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 131 keys, vi/en in sync". Live in-browser QA could not complete in the sandbox preview browser (it can't reach the backend `loadAppData` fetches, so the app stays on its data loader); modules serve 200 with no runtime errors. Real visual/interactive confirmation (autocomplete filter + keyboard, fly-to on select, floating toggle, basemap list, LangToggle sweep) still owed to a manual check by the user via their own `npm run dev`.
- follow-up: `expand_less.svg?raw` (first icon choice) doesn't exist in `@material-symbols/svg-400` and `tsc` didn't catch it — only Vite's resolver did; fixed with `keyboard_arrow_up/down`. Search still keys on muid only (no gazetteer/place-name source exists).

## 2026-07-24 — Follow-up (2nd) Dock the GIS layer panel (push map, no overlay)
- changed: re-docked the left "Lớp dữ liệu" panel so opening it pushes the map narrower instead of floating a translucent card over it (user found the overlay covered the map + scrolled despite few options).
  - `GisMap.tsx`: `<GisLayerPanel>` moved out of `GisMapCanvas`'s `children` to a direct flex child of `.gis-body` (rendered before the map), gated `!focusMode && showLayerPanel`, with `onClose={() => setShowLayerPanel(false)}`. The old `.gis-layer-overlay`+`.gis-layer-toggle-btn` combo replaced by a single labeled `.gis-layer-open-btn` pill ("Lớp dữ liệu") floated bottom-left of the map, shown only while the panel is closed.
  - `GisLayerPanel.tsx`: added `onClose` prop + a `.gis-layer-panel-head` (title `gis.layer.panelTitle` + close `chevron-left` button). Groups/rows unchanged.
  - `styles.css`: `.gis-body` now `display:flex; gap:12px`; `.gis-layer-panel` restyled from floating to a solid full-height docked sidebar (`flex:none; width:244px; align-self:stretch; border`); removed `.gis-layer-overlay`/`.gis-layer-toggle-btn`, added `.gis-layer-panel-head/-title/-close` + `.gis-layer-open-btn`; updated the `prefers-reduced-motion` selector list; fixed 2 now-stale `.gis-layer-overlay` comment refs (here + `GisRightPanel.tsx`). Map resize on open/close is handled by the map's existing P2-03 `ResizeObserver`.
  - Panel defaults closed (map-first) — opening is the deliberate "đẩy map vô" action the user described.
- files: `web/src/pages/GisMap.tsx`, `web/src/components/gis/{GisLayerPanel,GisRightPanel}.tsx`, `web/src/styles.css`, `web/src/i18n/strings.ts`, `docs/learn-log/FOLLOWUP-2026-07-24-gis-search-toggle-basemap.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 132 keys, vi/en in sync"; no stale `.gis-layer-overlay`/`.gis-layer-toggle-btn` code refs. Live visual QA still owed to the user's own `npm run dev` — the sandbox preview browser can't reach the backend `loadAppData` fetches.
- follow-up: on very narrow viewports the 244px docked panel + map could get tight; no mobile breakpoint added yet. Panel default-closed — flip `showLayerPanel` initial state to `true` if the user wants it open on load.

## 2026-07-24 — Phase 3 (Quan trắc thời gian thực) — single-tab redesign, full page
- decision: user revised Phase 3 → **1 trang duy nhất** (bỏ 9 sub-tab), đúng **5 nhóm**: bản đồ trạm quan trắc · bảng mưa thực đo · bảng mực nước theo cống (Tên cống/ngoài sông/trong cống/đóng-mở) · biểu đồ diễn biến mưa (toggle 10p/giờ/ngày, chọn ≤5 trạm) · top-10 + phân bố theo khung giờ. Per-trạm data KHÔNG có trong Supabase → user chốt **seed synthetic vào Supabase** (đúng kỷ luật "data-in-DB, không mock JSON ở frontend").
- changed (data layer):
  - **P3-D1** migration `web/supabase/migrations/20260724120000_monitoring_stations.sql`: bảng `rain_stations` (code/name/geom/elevation/status/battery/signal + `rain_10min numeric[]`) + `culverts` (`river_series`/`inside_series`/`gate_series numeric[]`), chuỗi 10 phút/144 bước gốc 00:00 (khuôn như `simulation_node_fill.fill_series`); view `rain_stations_geojson`/`culverts_geojson` (ST_AsGeoJSON); bật RLS + policy SELECT anon/authenticated + REVOKE ghi. Hand-add 2 bảng + 2 view vào `web/src/lib/database.types.ts` (không có live DB để regen).
  - **P3-D2** `data-pipeline/generate_monitoring_data.py` (seeded Random(42): 15 trạm mưa địa danh VL với hyetograph gamma chuẩn hoá về total24h mục tiêu để top-10 có nghĩa; 10 cống với chuỗi triều + đáp ứng bể chứa tuyến tính + logic cửa đóng khi sông>cống+biên) → `shared/data/rain-stations.json`/`culverts.json`; mở rộng `import_dynamic_data.py` (`import_rain_stations`/`import_culverts`, `ST_MakePoint`).
- changed (frontend):
  - **P3-01** `App.tsx` route `/monitoring` → `Monitoring` (bỏ `:tab`, redirect link cũ); `types.ts` (+`RainStation`/`Culvert`, mở rộng `AppData`); `loadData.ts` (+`loadRainStations`/`loadCulverts`, degrade `[]` khi view chưa seed để không brick toàn app); `data/monitoringService.ts` mới (dẫn xuất thuần: `currentMonitoringStep` neo đồng hồ, `rainWindowSum` cuộn vòng, bảng/top-10/phân bố/diễn biến/cống, `RAIN_BUCKETS`+`rainColor`).
  - **P3-02..P3-06** components mới trong `web/src/components/monitoring/`: `MonitoringStationMap` (MapLibre, marker DOM tô màu mưa 24h + legend + dropdown lớp, tái dùng khuôn init/ResizeObserver của GisMapCanvas), `RainTable` (sort + aria-sort), `CulvertTable` (badge icon+màu), `RainTrendChart` (line đa trạm + toggle + chọn ≤5 trạm), `TopRainChart` (thanh ngang), `HourlyDistributionChart` (cột gộp Tổng+3 trạm). `pages/Monitoring.tsx` viết lại compose 5 nhóm.
  - **P3-07** i18n `mon.*` (2 khối vi/en cùng lúc) + CSS `mon-*` trong `styles.css` (tabular-nums cho số liệu, grid responsive, badge, marker, legend).
- files: `web/supabase/migrations/20260724120000_monitoring_stations.sql` (new), `data-pipeline/{generate_monitoring_data.py (new),import_dynamic_data.py}`, `shared/data/{rain-stations,culverts}.json` (generated), `web/src/lib/database.types.ts`, `web/src/{types.ts,loadData.ts,App.tsx}`, `web/src/nav/navItems.ts`, `web/src/data/monitoringService.ts` (new), `web/src/components/monitoring/*` (6 new), `web/src/pages/Monitoring.tsx`, `web/src/i18n/strings.ts`, `web/src/styles.css`, `docs/learn-log/P3-monitoring-single-tab.md` (new), `docs/learn-log/README.md`, `docs/LEARNING_LOG.md`
- verify: `cd web && npx tsc --noEmit` clean; `node scripts/check-i18n.mjs` — "OK - 163 keys, vi/en in sync"; `npm run build` ✓ (21s); generator prints 15 stations + 10 culverts, VLM_10 sum24h=80.9 khớp target. Live in-browser QA NOT done: (a) sandbox có không headless browser, (b) dữ liệu chưa nạp lên Supabase live (cần `SUPABASE_ACCESS_TOKEN`+`--project-ref` của user để áp migration + chạy importer). Trang degrade sang empty-state cho tới khi seed.
- follow-up: hoãn các sub-tab cũ P3-06…P3-09 (mực nước riêng/bơm/camera/radar/CLN/IoT/tổng quan). "Mưa ngày" hiển thị 6×4h vì demo chỉ có 24h dữ liệu. Station names là danh từ riêng (giữ nguyên VI/EN). Owed to user: áp migration + importer, rồi QA mắt + LangToggle sweep qua `npm run dev`.
